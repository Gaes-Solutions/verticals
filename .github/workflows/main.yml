name: Main Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy-main
  cancel-in-progress: false

jobs:
  deploy-staging:
    name: Deploy to staging (placeholder)
    runs-on: ubuntu-latest
    needs: []
    if: github.event_name == 'push'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: TODO — Hito 0.10/0.11 Hetzner+Coolify deploy
        run: |
          echo "Deploy a staging.gaessoft.com — pendiente hito 0.10 (Hetzner+Coolify)"
          echo "Commit: ${{ github.sha }}"
          echo "Ref: ${{ github.ref }}"
