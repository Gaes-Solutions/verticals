# CHANGELOG — GaesSoft POS

Formato basado en [Keep a Changelog](https://keepachangelog.com/es-ES/1.1.0/).
Versionado [SemVer](https://semver.org/lang/es/) (aplica desde primer release).

## [Unreleased]

### Hito 1 (POS Core retail) — arranque
- **2026-05-15** — Hito 1 arranca con `docs/hitos/hito-1-pos-core.md`: scope cerrado (núcleo POS retail vendible que reemplaza Eleventa para 1-2 clientes piloto), checklist de 7 sub-tareas demoables, performance targets explícitos (búsqueda producto P95 <100ms, checkout sin CFDI P95 <500ms, timbrado CFDI P95 <3s), demo objetivo (login → POS → multi-pago → ticket → CFDI → corte Z). Sub-tareas: 1.1 Modelo 4.6 Usuarios+Sucursales+Cajas+RBAC, 1.2 Modelo 4.7 Productos+Inventario+Motor precios, 1.3 Modelo 4.9 Ventas básicas+Multi-pago+Tickets, 1.4 Modelo 4.11 Cortes X/Z, 1.5 Modelo 4.19 CFDI+Facturama+Autofacturación QR, 1.6 Print Bridge Tauri V1, 1.7 Demo end-to-end. Las 3 tareas externas pendientes de Hito 0 (0.10/0.11/0.12) corren en paralelo (no bloquean código).
- **2026-05-26** — 🎉 **HITO 3 COMPLETO** (3.6 Demos end-to-end CERRADO): 3 scripts CLI nuevos en `apps/api/scripts/` que ejecutan los verticales contra la API LIVE con asserts paso a paso, registrados como `pnpm --filter @gaespos/api demo:clinica-vet|demo:abarrotes|demo:despacho`. **`demo-clinica-vet.ts`** (15 pasos verde): tenant salud → equipo clínico → catálogos (55 CIE-10 + 12 vacunas) → paciente humano → consulta SOAP firmada inmutable → receta QR validada → mascota vet → vacuna antirrábica + cartilla próxima dosis → camas → hospitalización (cama→ocupada + cargo estancia auto) → medicación c/8h×5d expande 15 kardex → enfermera aplica → signos vitales T°39.5+SatO2 89% dispara alertas rule-based fiebreAlta+hipoxemia → alta genera venta $2,500. **`demo-abarrotes.ts`** (8 pasos verde, requiere `RECARGA_PROVIDER=mock`): recarga Telcel cobra con margen + Bait pospago + saldo prefondeado descontado + producto IEPS 160% con desglose fiscal IEPS aparte de IVA en venta. **`demo-despacho.ts`** (9 pasos verde): upload CFDI XML combustible → auto-categorización IA mock confianza 0.95 → G-606 → OC → recepción vincula CFDI → DIOT TXT formato SAT separador `|` + programa partners (link → click cookie 90d → referral paying plan starter → comisión 25% $124.75 → aprobar → payout SPEI). **Soporte `RECARGA_PROVIDER=mock` agregado a `server.ts`** (mismo patrón que `FISCAL_PROVIDER=mock`); AI cae a MockAiProvider sin `ANTHROPIC_API_KEY`. Bugs menores corregidos en construcción (login tenant usa tenantSlug en body; validar receta requiere token tenant; desglose IEPS en GET detalle venta; DIOT TXT via fetch directo; `export {}` aísla scope global de scripts). Typecheck + lint verde, suite 481 tests sigue verde. **Hito 3 status: 3.1✅ 3.2✅ 3.3✅ 3.4✅ 3.5✅ 3.6✅ — los 5 clientes piloto tienen su vertical operativo en backend.** Pendiente: commit checkpoint + tag `hito-3-verticales-v1` (espera visto bueno de Gaby).

- **2026-05-26** — 🎉 **Hito 3.5 Partners + Despacho contable CERRADO al 100%** (portal partner frontend + conciliación bancaria + Facturama webhook diferidos a V1.5/Hito 6): **schema master 4.2 Partners** (7 modelos + 7 enums, migration `add_partners` 276 SQL: `Partner` codigo+RFC unique+nivel [bronze/silver/gold/diamond]+comisionPctOverride+estado [invitado/activo/pausado/terminado]; `PartnerBranding` slug público + dominio white-label V1.5; `PartnerLink` slug+UTM+contadores clicks/signups/paidConversions; `Referral` cookie attribution 90d+estado [click/signup/trial/paying/churned]+tenantId@unique+atribucionExpiraEn; `Commission` periodoYYYYMM+montoBaseTenantPaid+porcentajeAplicado+montoComision+estado [pendiente/aprobada/pagada/rechazada/disputada], unique [referralId,periodo]; `Payout` agrupa commissions+retencionISR/IVA+metodoPago [spei/paypal/stripe_connect]; `PartnerInvitacion` token+expira). **Schema tenant 4.12 Despacho** (6 modelos + 7 enums, migration `add_despacho_contable` 232 SQL: `CfdiRecibido` uuidSat unique+tipoComprobante [I/E/N/P/T]+emisor/receptor+subtotal/iva/isr/ieps desglosados+xmlRaw+origen [upload_manual/facturama_retrieve/webhook]+estado [vigente/cancelado]; `CategoriaContable` codigoContable cuenta SAT+tipo [activo/pasivo/capital/ingreso/gasto/costo]+esDeducibleSat+ivaAcreditable+isrRetenibleDefault+claveProdServSatRegex para heurística+jerarquía padre/hijo; `CfdiRecibidoCategorizacion` cfdiRecibidoId@unique+categorizadoPor [ia/regla_heuristica/manual]+iaModelo+iaConfianza+iaJustificacion+override; `OrdenCompra` folio OC-{CODIGO}-NNNNNN+state machine [borrador/enviada/recibida_parcial/recibida_total/cancelada]+vincula CfdiRecibido; `OrdenCompraLinea` con cantidadRecibida; `OrdenCompraFolioCounter`). **Seed 30 categorías contables MX** con regex claveProdServ SAT (G-601 a G-999 gastos, A-101 a A-105 activos, C-401 a C-403 costos, G-999 catch-all). **9 permisos nuevos**: CFDIS_RECIBIDOS_LEER/UPLOAD/CATEGORIZAR/CANCELAR + COMPRAS_OC_LEER/CREAR/AUTORIZAR/RECIBIR + DIOT_GENERAR (contador_interno + dueño/gerente todos; almacen LEER/CREAR/RECIBIR OC). **Paquete nuevo `@gaespos/ai`**: `AiProvider` interface (categorize+summarize) + `AnthropicClient` Claude Haiku 4.5 con **prompt caching** (system message cache_control ephemeral, crítico costos) + parseo JSON robusto + validación código contable existe + fallback IA→heurística ante error; `MockAiProvider` determinista para tests/dev; `categorizeByHeuristic` (regex claveProdServ→categoría, fallback G-999); 6 unit tests. Plugin Fastify `aiProviderFactory` decorator (cae a MockAiProvider sin `ANTHROPIC_API_KEY`). **Módulos `apps/api/.../`**: `cfdis-recibidos/` (parser CFDI 4.0 puro con regex sin deps externas extrae UUID/emisor/impuestos/conceptos; upload idempotente por uuidSat; `categorizarCfdi` IA con fallback heurístico + override manual con flag; cancelar 409 si ya cancelado); `ordenes-compra/` (crearOc con totales Decimal + autorizar borrador→enviada + recibir parcial/total con validación cantidad excedida 409 + vincular CfdiRecibido); `diot/` (`generarDiot` agrupa CFDIs vigentes tipo I categorizados con ivaAcreditable por RFC tercero + `reporteATxt` formato SAT separador `|` 11 campos UTF-8 sin BOM; endpoint export con Content-Disposition); `partners/` master (crearInvitacion token→aceptarInvitacion activa partner; crearLink; registrarClick cookie 90d idempotente; onTenantCreated asocia tenant→referral signup; transicionReferralEstado; recalcularNivelPartner por umbrales 5/15/30 paying; recalcularComisionesPeriodo idempotente por [referral,periodo] con pct nivel/override; aprobar/rechazar; crearPayout agrupa aprobadas; marcarPayoutPagado cascada commissions→pagada). Endpoints `/partners/*` admin + `/p/{slug}/click` y `/p/{slug}/accept-invite` públicos. **82 tests integración nuevos** (18 partners flujo completo invitación→payout + cookie attribution + recalcular nivel + comisión 25% + RBAC 401; 25 despacho: upload válido/idempotente/inválido 400 + auto-categoría IA G-606 + fallback heurístico + override manual + cancelar 409 + OC recepción parcial→total + excede 409 + DIOT TXT formato + RBAC 403; 6 unit AI; otros). **Suite total: 481 tests verde.** **Diferidos V1.5+**: portal partner frontend (→Hito 6 registro público), conciliación bancaria CFDIs vs estado cuenta, white-label dominio custom, ISR/IVA auto-retención payouts, BullMQ cron auto-recalcular comisiones (V1 manual via endpoint), Facturama webhook real-time recibidos (V1 cron mensual + upload manual), dispersión SPEI automática.

- **2026-05-25** — 🎉 **Hito 3.4 Salud N3 hospitalización CERRADO al 100%** (laboratorio + imagenología + push notifications diferidos a V1.5+): schema tenant 4.16 con 6 modelos nuevos + 6 enums + polimorfismo XOR paciente/mascota (`Cama` codigo único per-sucursal, 5 tipos [general/cuidados_intensivos/aislamiento/cirugia_recuperacion/pediatria], 5 estados [libre/ocupada/limpieza/mantenimiento/fuera_de_servicio], tarifaPorNoche default; `Hospitalizacion` folio HOSP-{CODIGO}-NNNNNN, paciente XOR mascota, medicoResponsable, diagnosticoIngreso CIE-10+diagnosticoIngresoTexto fallback, motivoIngreso, fechaIngreso/Egreso, altaPorId, observacionesAlta, **ventaAlAltaId @unique** 1:1 con Venta de alta, estado [activa/alta/fallecimiento/fuga/traslado_externo], tarifaEstanciaDiaria snapshot; `HospitalizacionFolioCounter` atómico; `MedicacionProgramada` con hospitalización+medicamentoCatalogoId+snapshot nombre+**dosis capturada por médico NO IA decide**+via+frecuenciaHoras+horaInicio+duracionDias+indicacionMedica+recetaId? opcional+estado [activa/suspendida/completada]+suspendidaAt+motivoSuspension; `KardexAplicacion` append-only con horaProgramada/horaAplicada/enfermeraAplicadorId+estado [pendiente/aplicada/omitida/reprogramada]+motivoOmision+reaccionAdversaObservada+**alertaEnviadaAt** anti-spam worker; `SignoVitalHospital` granular con paciente XOR mascota+capturadoPor+hora+T°/FC/FR/SatO2/PA sist+diast/glucosa/dolor escala/llenadoCapilar/mucosasColor+observaciones+**alertasMarcadas JSONB** rule-based; `CargoHospital` append-only con tipo [estancia_diaria/medicamento/procedimiento/laboratorio/imagenologia/consumible/honorarios_medicos/otro]+descripcion+cantidad+precioUnitario+monto Decimal preciso+**facturadoEnVentaId @nullable + facturadoAt** marcado al darAlta). **8 permisos nuevos**: CAMAS_LEER/GESTIONAR + HOSPITALIZACION_LEER/CREAR/ALTA + MEDICACION_PROGRAMAR + KARDEX_LEER/APLICAR/REPROGRAMAR; distribuidos dueño/gerente todos, médico LEER/CREAR/PROGRAMAR+KARDEX_LEER/APLICAR/REPROGRAMAR (puede capturar signos y aplicar en emergencia), enfermera LEER+KARDEX_LEER+APLICAR (rutina frontline), recepción LEER+ALTA+CAMAS_GESTIONAR (cambio limpieza→libre)+KARDEX_LEER. Migration `add_salud_hospitalizacion` (278 SQL) vía CLI shadow-database aplicada cross-tenant. **Módulos `apps/api/.../`**: `camas/` CRUD con cambio-estado bloqueado si tiene hospitalización activa 409; `hospitalizaciones/` service complejo (`ingresarPaciente` atómico valida cama libre+isActive+misma sucursal → cama→ocupada → folio HOSP-* → cargo estancia_diaria auto con tarifa cama; `programarMedicacion` médico captura dosis/frecuencia/duración → **expande N entradas KardexAplicacion** según frecuenciaHoras×duracionDias; `aplicarKardex` enfermera con append-only 409 si re-aplica, omitida requiere motivoOmision 400, reprogramada requiere nuevaHoraProgramada 400; `capturarSignoVital` con `evaluarAlertasSignoVital` rule-based: fiebreAlta T≥39, hipotermia T≤35, taquicardia FC>180, bradicardia FC<40, hipoxemia SatO2<90, hipertensión PAsist>180, hipoglucemia<60, hiperglucemia>250 — **NO IA decisional**; `agregarCargo` Decimal cantidad×precioUnitario; `darAlta` atómico suspende meds activas → cama→limpieza → si generarVenta!==false && cargos no facturados crea **Venta borrador** folio normal con total=suma cargos + ventaAlAltaId poblado + CargosHospital marcados facturadoEnVentaId+facturadoAt). **Worker `apps/api/src/workers/medicacion-alarmas.ts`**: `AlarmChannel` interface (testeable sin BullMQ) + `InMemoryAlarmChannel` para tests + `escanearKardexParaAlarmar(client, channel, opts)` query kardex pendiente+alertaEnviadaAt IS NULL+horaProgramada en ventana [ahora, ahora+15min default]; valida med+hosp activos; envía via channel; marca alertaEnviadaAt=ahora (anti-spam: 2do escaneo no re-envía). V1 in-process; V1.5 agrega BullMQ+Redis cron+push notifications app móvil (Hito 5). **Decisión Venta sin VentaLinea**: para evitar migration adicional a VentaLinea (requiere productoId+varianteId NOT NULL), la Venta de alta se crea sin líneas con total=suma cargos; frontend consume desglose via relación `cargosHospitalFacturados`. **30 tests integración** cubriendo CRUD camas + ingreso humano + mascota vet + XOR completo (juntos 400, ninguno 400, ocupada 409) + listado filtra mascotaId + **medicación expande 15 kardex c/8h×5d verificado** + RBAC enfermera 403 programar + aplicar/omitir/reprogramar + append-only 409 + suspender (query Prisma directo verifica estado) + **signos vitales con alertas rule-based** + worker alarmas anti-spam + cargos manuales Decimal + **darAlta crea Venta borrador con total exacto + cama limpieza + cargos marcados facturados** + alta duplicada 409 + cama limpieza→libre por recepción. Suite total: **358 tests verde** en 116s. Lint sin errores. Hito 3 status: 3.1✅ 3.2✅ 3.3✅ 3.4✅. **Diferidos a V1.5+**: Laboratorio (EstudioLaboratorio + integración IDEXX vet / referencia humano), Imagenología (DICOM viewer + JPG; **IA hallazgos NO** por regla línea roja), Expo-push notifications app móvil enfermería (→ Hito 5 multi-plataforma), Hoja de evolución diaria firmada NOM-024 N3, Triaje IA (línea roja — NO).

- **2026-05-22** — 🎉 **Hito 3.3 Salud Vet CERRADO al 100%** (hospitalización N3 + laboratorio + imagenología diferidos a 3.4/V1.5): schema tenant 4.15 vet con 3 modelos nuevos + 3 enums + polimorfismo XOR en Cita/Consulta/Receta/ConsultaSignoVital (`Mascota` con numeroExpediente único MAS-NNNNNN + especie [perro/gato/ave/conejo/huron/reptil/pez/roedor/otro] + raza + sexo + esEsterilizado + fechaNacimiento [flag aproximada] + color + **microchip** + pesoActualKg + tutorClienteId FK opcional Cliente B2C + medicoAsignadoId + alergias/antecedentes/medicamentos crónicos JSONB + fechaDefuncion + causaDefuncion + archivable; `VacunaCatalogo` con nombre comercial + principio activo + tipo + aplicaHumano/Vet flags + especiesAplicables JSONB + esquemaDefault{Humano,Vet} JSONB + viaAplicacion + **intervaloRefuerzosDias** clave para cartilla determinista + isObligatoriaCartillaNacional flag SSa MX; `Vacunacion` con paciente XOR mascota + vacunaCatalogoId + medicoAplicador + **numeroLote + caducidadLote obligatorios para rastreabilidad ante retiros del mercado** + marcaSnapshot + viaAdministracion + dosisAplicada + reaccionAdversaObservada + proximaAplicacionFecha calculada determinista). Polimorfismo XOR validado en capa servicio Zod `.refine()` + checks explícitos en routes (NO constraint DB porque Prisma no lo soporta nativo); ALTER TABLE hace pacienteId nullable y agrega mascotaId + relación inversa + índices `@@index([mascotaId, fechaConsulta])` para queries vet. 3 enums nuevos: `MascotaEspecie` (9 valores), `MascotaSexo` (macho|hembra|desconocido), `CartillaTipo` (humano_basico_ssa|complementario|vet_perro|vet_gato|vet_otro — preparado para `CartillaPaciente` model en 3.4). **7 permisos nuevos**: MASCOTAS_LEER/CREAR/ACTUALIZAR/ARCHIVAR + VACUNAS_LEER/APLICAR/GESTIONAR_CARTILLA; distribuidos dueño/gerente todos, médico todos excepto archivar/gestionar, enfermera lectura+aplicar, recepción CRUD básico + aplicar vacuna (en consultorios pequeños la recepcionista frecuentemente aplica bajo supervisión médica). Migration `add_salud_vet` (189 SQL) vía CLI shadow-database aplicada cross-tenant. **Seed catálogos vet** integrado a `seedClinicalCatalogs`: 25 CIE-10 vet (V-001 a V-025: parvovirosis canina, moquillo, calicivirus felino, rinotraqueítis, dermatitis atópica, sarna sarcóptica, otitis, IRC, diabetes felina, hipertiroidismo, atropello, cuerpo extraño GI, etc.); 12 medicamentos vet PLM (Drontal Plus, Bravecto, NexGard, Synulox, Marbofloxacina, Meloxicam Vet, Rimadyl, Cerenia, Frontline Plus, Caninsulin, Apoquel, Acepromacina) con `dosisRecomendadaVet` JSONB por especie con mg/kg + frecuencia de REFERENCIA (NO IA calcula); 12 vacunas catálogo (humanas SSa: BCG, HepB, Pentavalente, SRP, Influenza, Td, VPH + vet: Vanguard Plus 5/CV-L, Antirrábica canina, Bordetella, Triple felina FVRCP, Leucemia felina FeLV) con esquemasDefault JSONB y intervaloRefuerzosDias. **Módulos `apps/api/.../`**: `mascotas/` CRUD con búsqueda multi-criterio (nombre/expediente/microchip/raza), filtro por especie/tutor/médico, archivar, generador MAS-NNNNNN, defunción via PATCH; `vacunaciones/` con `procesarAplicacion` validando XOR + vacunaCatalogoId existente, próxima fecha calculada determinista de `intervaloRefuerzosDias`, GET `/cartilla?pacienteId=` XOR `?mascotaId=` retorna sujeto + vacunaciones aplicadas con estado [vigente|proxima(≤30d)|vencida] + lista `proximasDosis` ordenada con diasFaltantes; `consultas/diagnosticos/catalogo` extendido con query `?vertical=humano|vet|todos` (default todos); Citas/Consultas/Recetas routes con validación XOR explícita (400 si ambos o ninguno) + includes de mascota selects (id/nombre/especie/numeroExpediente). **19 tests integración** (`tenant-salud-vet.test.ts`): catálogos vet sembrados (CIE V-007 parvovirosis, Bravecto vet, Antirrábica canina + Triple felina con `aplicaVet=true`); CRUD mascota con búsqueda por microchip + filtro por especie + PATCH peso; **flujo full** cita mascota (sin pacienteId) → checkin → consulta SOAP vet con dx parvovirosis firmada → receta vet con Meloxicam (QR muestra "Firulais (perro)" sin paciente) → vacuna Antirrábica con lote+caducidad + próxima dosis +365 días determinista; cobertura XOR (cita 400 si paciente+mascota juntos, 400 si ninguno; consulta 400 mismo; vacunación 400 mismo); cartilla con vigente+vencida+proximasDosis ordenadas; permisos recepción aplica vacuna OK pero NO DELETE registro cartilla 403. Suite total: **328 tests verde** en 102s. Lint sin errores. Hito 3 status: 3.1✅ 3.2✅ 3.3✅. **Diferidos a 3.4 / V1.5+**: hospitalización N3 (Cama virtual + AsignacionHospital + KardexAplicacion + MedicacionProgramada + alarmas push BullMQ + SignosHospital + CargosHospital), laboratorio (EstudioLaboratorio + integración IDEXX), imagenología (DICOM + JPG viewer + IA hallazgos), pediatría vet (curvas crecimiento por raza), `CartillaPaciente` model cacheado (V1 computed on-the-fly), resumen IA visitas al abrir expediente (Hito 4 IA features), Doctoralia perfil vet público (Hito 4).

- **2026-05-22** — 🎉 **Hito 3.2 Salud Humana base CERRADO al 100%** (PHR cross-tenant + vacunas + pediatría diferidos a V1.5): schema tenant 4.15 V1 con 10 modelos + 7 enums (`Paciente` con numeroExpediente único EXP-NNNNNN + datos demográficos completos + alergias/antecedentes JSONB + tutorClienteId FK opcional Cliente B2C + clasificacionRiesgo capturada por médico; `Medico` extensión 1:1 Usuario con cédula+especialidades+firma+telemedicina; `Agenda` + `AgendaBloqueo` con tipos vacaciones/congreso/personal/incapacidad/cerrado_sucursal; `MotivoCitaCatalogo`; `Cita` con state machine programada→confirmada→checkin→en_consulta→completada/cancelada/no_asistio + signos vitales recepción; `Consulta` SOAP central con diagnóstico CIE-10 capturado manual + diferenciales + pronóstico + estado [borrador|firmada|enmendada|cancelada] **inmutable post-firma NOM-024**; `ConsultaSignoVital` historial granular; `DiagnosticoCatalogo` CIE-10/11; `MedicamentoCatalogo` con clasificacionCofepris [G_I-VI|vet|OTC] + dosisRecomendada{Pediatrica,Adulto,Vet} JSONB de **REFERENCIA** (NO IA calcula); `Receta` con QR token + `esGrupoControlado` + numeroRecetarioOficial COFEPRIS obligatorio para G_II/III; `RecetaItem` con snapshot + dosis manual del médico). 3 roles preset nuevos medico/enfermera/recepcion + 18 permisos clínicos distribuidos. Migration `add_salud_humana` (549 SQL la mayor del proyecto) cross-tenant. **Seed catálogos clínicos** nuevo (30 CIE-10 + 25 medicamentos PLM con dosis referencia + 8 motivos de cita) integrado en `seedTenantDefaults`. Módulos `apps/api/.../{pacientes,medicos,agenda,citas,consultas,recetas}/`: pacientes CRUD con búsqueda multi-criterio + generador EXP-NNNNNN; medicos PUT upsert perfil clínico; agenda CRUD horarios + bloqueos con validación fechas; citas state machine con transiciones validadas + checkin con signos vitales y tiempoEspera computado automático; consultas SOAP `crear→editar (solo borrador)→firmar (inmutable)→enmendar (clona vinculada)` con PATCH a firmada 409; recetas con detección automática G_II/III + QR token único 48 hex + validación pública por token con flag `vigente`. **27 tests integración** cubriendo seed catálogos + CRUD pacientes + flujo full agenda→cita→consulta firmada→receta QR + G_II sin recetario 409 + enmienda clona y marca original + RBAC enfermera/recepción → 403. Suite total: **309 tests verdes** en 92s. Lint sin errores. Hito 3 status: 3.1✅ 3.2✅. **Diferidos V1.5+**: PHR cross-tenant master DB FHIR + vacunas+cartilla SS MX + pediatría (curvas OMS + hitos desarrollo) + motor reglas interacciones automático + telemedicina Daily.co (Hito 4) + Doctoralia perfil público (Hito 4) + vector embeddings búsqueda semántica + validación cédula vs SSa API.

- **2026-05-21** — 🎉 **Hito 3.1 Abarrotes (recargas + IEPS + Bait pago) CERRADO al 100%**: schema tenant 4.14 con 4 modelos + 4 enums (`RecargaProveedorConfig` per-tenant con saldoPrefondeado+alerta+api_key encrypted; `Recarga` folio único RC-{CODIGO}-NNNNNN, 10 compañías V1 incl. Bait pospago, tipo tiempo_aire|pago_servicio, montoCobrado vs solicitado con comisión separada, folioProveedor para reclamos, estado [pendiente|exitosa|fallida|reembolsada|disputada], reembolsadaAt+motivo, disputadaAt+motivo, link a cajaApertura+venta opcional; `RecargaReintento` append-only; `RecargaFolioCounter`). 4 permisos nuevos RECARGAS_LEER/VENDER/REEMBOLSAR/CONFIGURAR distribuidos por preset. Migration `add_recargas` (145 SQL) cross-tenant. **Paquete nuevo `@gaespos/recargas`** con `RechargeProvider` interface + `MockRecargaProvider` determinista con idempotency memo (17 unit tests) + `RecargaKiClient` stub V1 + catálogo V1 hardcoded (9 compañías tiempo aire MX + Bait pospago) + helpers `validarMonto`/`validarNumeroMx`. Plugin Fastify `recargaProviderFactory` mismo patrón que fiscal. Módulo `apps/api/.../recargas/`: service con `procesarRecarga` (valida número MX + monto compañía + apertura caja + saldo suficiente + retry 1x automático ante falla primera, idempotency key derivado de recarga.id, RecargaReintento por intento, actualiza saldo solo si exitosa), `reembolsarRecarga` (solo fallidas/disputadas), `marcarDisputada`, `consultarSaldos` con flag bajo. 8 endpoints `/t/recargas/*` (catálogo público no expone mock, saldos por proveedor, PUT upsert config, list paginado, detalle, procesar, reembolsar, marcar-disputada). **23 tests integración** + **4 tests IEPS** = 27 nuevos. **IEPS motor completo**: descomposición porcentaje (cigarro 160%: base/(1+iepsPct)/(1+ivaPct); IVA sobre IEPS según SAT) y cuota_por_unidad (refresco $1.5375/L: IEPS fijo no entra a base IVA), `tasaIeps` JSONB `{tipo, valor}` validado, CFDI 4.0 pasa `aplicaIeps+tasaIeps` por concepto (calculado proporcionalmente). Suite total: **282 tests verdes** en 73s. Hito 3 status: 3.1✅ — siguiente 3.2 Salud Humana base. Diferidos V1.5/V2: cron monitor saldo bajo BullMQ + WhatsApp alert, RecargaKi API real cuando Gaby contrate, expansión servicios CFE/SIAPA/Telmex reusará schema con tipo=pago_servicio.

- **2026-05-20** — 🎉 **Hito 2.7 Demo comercial end-to-end CERRADO · Hito 2 Comercial al 100%**: nuevo script `apps/api/scripts/demo-comercial.ts` (~620 líneas) registrado como `pnpm --filter @gaespos/api demo:comercial`, complementa el `demo:retail` de Hito 1 sin duplicar setup. **20 pasos verificados en corrida live contra API real con mock fiscal**: setup tenant+4 usuarios (owner/cajero/vendedor/almacen) → CFDI sandbox → 3 productos obra (cemento/tubo PVC/grava) con stock 200 → apertura caja → **apartado** 5 cementos con abono inicial + 2 abonos parciales + liquidación que crea venta y libera reserva → **venta fiado** $1,392 + **regularización fiado→CxC** $1,000 con interés mora → **cotización B2B** $6,960 enviada por email → owner acepta → **convertir-pedido** con estadoAprobacion=pendiente (>$5K umbral) → almacen bloqueado por aprobación 409 → owner aprueba → almacen prepara→marca enviado con Estafeta+tracking+URL→marca entregado → **convertir-venta credito_b2b** crea CxC automática y consume línea (50K→43K disponible) → **devolución 5 defectuosos** con `nota_credito_cxc` reponeStock=false (merma net-zero) abona $1,160 a la CxC → **pago final CxC** $5,800 efectivo liquida la CxC. Fix infra: helper `call()` solo agrega `Content-Type: application/json` cuando hay body (Fastify rechaza POST con header pero sin body). **Hito 2 cerrado al 100%** — 7/7 sub-tareas verde, 255 tests + 2 demos CLI end-to-end funcionando. Próximos: tag `hito-2-comercial-v1`, decidir vertical inicial Hito 3.

- **2026-05-20** — Hito 2.6 Cotizaciones → Pedidos B2B cerrado: schema tenant 4.10 con 6 modelos + 4 enums (`Cotizacion` folio único QT-{CODIGO}-NNNNNN + estado [borrador|enviada|aceptada|rechazada|vencida|convertida] + canal envío [email|whatsapp|descarga|otro] + pdf placeholder V1 + pedidoId @unique 1:1; `CotizacionLinea` snapshot; `CotizacionFolioCounter`; `Pedido` folio PD-{CODIGO}-NNNNNN + estado [creado|preparando|enviado|entregado|cancelado] + estadoAprobacion [no_requiere|pendiente|aprobada|rechazada] derivado del cliente + ordenCompraCliente + direccionEnvioId FK + paqueteria/trackingExterno/trackingUrl + ventaId @unique; `PedidoLinea`; `PedidoFolioCounter`). **5 permisos nuevos** COTIZACIONES_LEER/ENVIAR/GESTIONAR_ESTADO + PEDIDOS_LEER/CREAR/APROBAR/GESTIONAR/CONVERTIR_VENTA, distribuidos en presets (gerente todos, vendedor crea+gestiona sin aprobar, cajero convertir-venta, almacen prepara/envía/entrega). Migration `add_cotizaciones_pedidos` (240 SQL) cross-tenant. Módulo `apps/api/.../cotizaciones/`: service con `crearCotizacion` (motor cascada → snapshots), `enviarCotizacion` (canal+destino+pdfPlaceholder), `aceptar`/`rechazar` con state machine 409. Módulo `apps/api/.../pedidos/`: service con `crearPedido` directo + `convertirCotizacionAPedido` (tx atómica, hereda estadoAprobacion según `cliente_b2b.requiereAprobacionInterna+montoAprobacionRequired`), `aprobar`/`rechazar`, state machine `marcarPreparando→marcarEnviado(paqueteria+tracking)→marcarEntregado`, `cancelar` (409 si entregado o ya ventaId), `convertirAVenta` (entregado → Venta canal=mayoreo + stock + valida credito_b2b vs línea + **crea CxC automática** + marca pedido.ventaId). 16 endpoints `/t/cotizaciones/*` y `/t/pedidos/*`. **17 tests integración nuevos**: vendedor crea borrador, **flujo full** cot→enviar→aceptar→convertir→preparar→enviar→entregar→convertir-venta con stock validado y pedido.ventaId, cliente con requiere_aprobacion umbral=5000 bloquea preparar hasta aprobar, owner aprueba→almacen prepara, rechazar pedido, vendedor sin PEDIDOS_APROBAR 403, state machine errors (aceptar borrador 409, convertir rechazada 409, entregar sin enviado 409, convertir no-entregado 409, doble convert 409, cancelar entregado 409), filtros lista, convertir-venta `credito_b2b` crea CxC y consume línea. Suite total: **255 tests verdes** en 64s. Diferidos V1.5+: PDF real Puppeteer+firma, recordatorios vencimiento BullMQ, tracking automático paqueterías, cancelación parcial líneas pedido.

- **2026-05-20** — Hito 2.5 Devoluciones con CFDI Egreso cerrado: schema tenant 4.9.d con 3 modelos + 4 enums (`Devolucion` folio único DV-{CODIGO}-NNNNNN + tipo [total|parcial] + 6 motivos + 7 métodos reembolso incluyendo `nota_credito_fiado` y `nota_credito_cxc` + `reponeStockDefault` + `aprobadoPorId` para V1.5 + estado [procesada|cancelada]; `DevolucionLinea` con snapshot + `ventaLineaId` + `reponeStock` per-linea override + `motivoLinea`; `DevolucionFolioCounter`). **Refactor Cfdi**: removido `@unique` de `ventaId` (una venta puede tener 1 Ingreso + N Egresos), agregados `devolucionId @unique` + `tipoRelacionSat` + `cfdiRelacionadoUuids[]` para soportar relación SAT tipo 03 (Nota de crédito). Extensión `@gaespos/fiscal` con `CfdiEmitirInput.cfdisRelacionados`. `MovimientoTipoFront` ahora incluye `devolucion_cliente` (signo +1). Permisos `cajero`/`vendedor` ganaron VENTAS_DEVOLVER por default (Square/Shopify retail standard). Migration `add_devoluciones` (140 SQL) vía CLI shadow-database introducido en 2.4 → aplicada cross-tenant. Módulo `apps/api/.../devoluciones/`: service `procesarDevolucion` (valida venta cobrada/no cancelada, valida método reembolso compatible, calcula cantidad ya devuelta acumulada por VentaLinea, prorrateo iva/ieps/subtotal por factor cantidadDev/cantidadOriginal, determina tipo total vs parcial, aplica stock con patrón net-zero cuando `reponeStock=false` (+devolucion_cliente −merma = 2 movimientos audit), aplica reembolso a fiado/CxC según método, emite CFDI Egreso vinculado al Ingreso vigente vía cfdisRelacionados tipoRelacion=03); 3 endpoints `POST /t/ventas/:id/devolver` + `GET /t/devoluciones` + `GET /t/devoluciones/:id`. **15 tests integración nuevos** cubriendo restock/merma/override per-linea/total vs parcial/cantidad acumulada 409/venta cruzada 400/venta cancelada 409/nota_credito_fiado reduce saldo (+400 sin clienteId+409 sin credito_fiado previo)/nota_credito_cxc abona CxC asociada/CFDI Egreso vinculado a Ingreso vigente con folioFiscal/sin Ingreso cfdiEgresoId=null/filtros lista/rol almacen sin VENTAS_DEVOLVER 403. **238 tests verdes** en 56s. Diferidos: cancelación devolución, aprobación umbralizada V1.5, devolución a proveedor, refund auto-Stripe/Conekta.

- **2026-05-20** — Hito 2.4 CxC formal cerrado: schema tenant 4.9.c con 3 modelos + 2 enums (`CuentaCobrar` folio único per-sucursal CXC-{CODIGO}-{NNNNNN} + tipoOrigen [venta_credito|regularizacion_fiado|manual|apertura_saldo_inicial] + clienteId XOR clienteB2bId + ventaId @unique opcional + vendedorId + comisionPagadaAVendedor flag + monto/pagado/currency + fechaEmision/Vencimiento/diasCreditoOtorgados + tasaInteresMoraPct/interesAcumulado + estado [activa|vencida|liquidada|incobrable|condonada]; `CxcPago` append-only con metodo VentaPagoMetodo; `CxcFolioCounter` atómico). Enum `VentaPagoMetodo` extendido con `credito_b2b`. 4 permisos nuevos CXC_LEER/CREAR/COBRAR/CONDONAR. **Bonus infra: CLI `pnpm migrate make <target> <name>` reutilizable** para generar migrations vía shadow database temporal (resuelve limitación de `prisma migrate dev` con multi-schema). Migration `add_cxc` (102 SQL) cross-tenant. Módulo `apps/api/.../cxc/`: service con `lineaCreditoDisponible` (autorizada - sum abiertas) + `validarCreditoB2bSuficiente` + `crearCuentaCobrar` + `crearCxcDesdeVentaB2b` + `registrarPago` (auto-liquida saldo=0) + `condonarCxc` + `marcarIncobrable` + CxcError estructurado; routes 8 endpoints `/t/cxc/*`. **Integración `crearVenta`**: pago `credito_b2b` valida línea ANTES de tx, crea CxC dentro de la tx con días+tasa heredados de la línea activa, rechaza cambio. **Regularización fiado→CxC** en fiado-service: tx única reduce saldo via FiadoMovimiento(regularizacion_cxc) + crea CxC tipoOrigen=regularizacion_fiado ligada via referenciaTipo/Id. 23 tests integración nuevos. Suite: **223 tests verdes** en 51s.

- **2026-05-20** — Hito 2.3 Apartados cerrado: schema tenant 4.9.b con 4 modelos + 2 enums (`Apartado` folio único per-sucursal AP-{CODIGO}-{NNNNNN} + estado [activo|liquidado_y_entregado|cancelado|expirado] + montoPagado acumulado + fechaLimite + penaCancelacionPct + politicaCancelacion snapshot; `ApartadoLinea` snapshot inmutable misma estructura que VentaLinea; `ApartadoAbono` append-only 6 métodos; `ApartadoFolioCounter` atómico). Extensión Venta con `apartadoId @unique` para relación 1:1. 5 permisos nuevos APARTADOS_*. Roles preset actualizados: gerente todos, cajero/vendedor sin CANCELAR. Migration cross-tenant. **Helpers inventario nuevos** `aplicarReservaApartado/liberarReservaApartado` que tocan stockReservado (no stockActual). Service `apartados/service.ts` con 4 ops atómicas: crearApartado (preview cascada + reserva por línea + abono inicial opcional, InsufficientStockError→409 con extra), registrarAbono (valida saldo), liquidarApartado (libera reserva + descuenta stockActual via aplicarAjuste + crea Venta linkada con folio normal copiando líneas/snapshot/abonos como pagos), cancelarApartado (libera reservas + pena=pagado*penaPct con override). 14 tests integración: reserva sin tocar stockActual, stock insuficiente→409 con extra.stockDisponible, abono>saldo→409, liquidar antes de saldar→409, liquidación crea venta y libera+descuenta atómico, re-liquidación→409, cajero sin APARTADOS_CANCELAR→403, cancelación con penaPct 25% calcula bien, override penaPct=0 para VIP. Diferidos: job nocturno apartado_expirado→V2 BullMQ, CFDI Egreso si abono facturado→2.5, pena en monedero→V1.5. Suite: 200 tests API + 22 perm + 16 pricing + 18 db + 3 fiscal = 259 verdes.

- **2026-05-20** — Hito 2.2 Clientes B2B + crédito formal cerrado: schema tenant 4.8.b con 6 modelos + 3 enums (`ClienteB2b` con razónSocial+RFC unique+régimen+nivelMayoreoId FK+listaPrecioPrincipalCodigo+diasCreditoDefault+condicionesPago enum+requiereOrdenCompra+formatoFacturaPreferido+requiereAprobacionInterna+montoAprobacionRequired; `ClienteB2bContacto` multi con esDecisor/esPagador flags; `ClienteB2bDireccion` multi con contactoRecepcion + horarioRecepcion + swap isDefaultEnvio; `ClienteB2bCredito` con max 1 activa por cliente, renovaciones archivan; `ClienteB2bListaPrecio` M2M con prioridad+vigencia; `ClienteB2bVendedorAsignado` PK compuesta tipo[principal|secundario|cobranza]+comisionPctOverride). Extensión `Venta`: nuevo `clienteB2bId` FK opcional. Migration `add_clientes_b2b_credito` cross-tenant. Endpoints `/t/clientes-b2b` CRUD + sub-recursos contactos/direcciones/credito/listas-precio/vendedores con upsert y validaciones de existencia. 15 tests integración cubriendo CRUD + RFC duplicate + búsqueda multi-criterio + 3 contactos con roles + 2 direcciones con swap + autorización crédito archiva anterior (max 1 activa) + lista inexistente→404 + multi-vendedor mismo cliente distinto tipo + venta con clienteB2bId persiste correctamente. Diferidos: documentos S3→V1.5, portal autoservicio→Hito 3, método pago credito_b2b→Hito 2.4 con CxC. Suite: 186 tests API + 22 perm + 16 pricing + 18 db + 3 fiscal = 245 verdes.

- **2026-05-20** — Hito 2.1 Clientes B2C + fiados cerrado: schema tenant 4.8 con 7 modelos + 3 enums (`Cliente` con flags fiscales MX + permite_fiado + limite_fiado + is_default; `ClienteDireccion`, `ClienteTelefono`, `ClienteGrupo`, `ClienteEtiqueta`, `Fiado`, `FiadoMovimiento` append-only). Migration `add_clientes_b2c_fiados` cross-tenant + migration `add_credito_fiado_metodo` agrega valor al enum VentaPagoMetodo. **Seed extendido**: cliente "Público en general" auto-creado por tenant. Service `fiado-service.ts` con `aplicarCargoFiado` (valida permiteFiado + límite, FiadoError 409), `aplicarAbonoFiado` (transacción, marca liquidado si saldo=0), `disponibleFiado` helper. Endpoints `/t/clientes` CRUD + búsqueda multi-criterio (?q= nombre/apellidos/rfc/email/teléfono) + GET /default + sub-recursos direcciones/teléfonos/etiquetas con swap automático + GET/POST fiado + grupos. Cliente público read-only (PATCH/DELETE → 403). **Integración cross-módulo en `crearVenta`**: detecta pago `credito_fiado`, requiere clienteId (400), prohíbe cambio, invoca aplicarCargoFiado en la transacción. 25 tests integración nuevos cubriendo: seed público read-only, CRUD con RFC case-insensitive + duplicate→409, búsqueda por nombre/RFC/teléfono, sub-recursos con swap default, fiados (venta dentro/excede límite, abono parcial/total/excede, cliente sin permiteFiado→409, venta a fiado sin clienteId→400), grupos. Diferidos: B2B (Hito 2.2), PHR (Hito 3), pgvector (V1.5), validación SAT API (V2), regularización fiado→CxC (Hito 2.4). Suite: 171 tests API + 22 perm + 16 pricing + 18 db + 3 fiscal = 230 verdes en ~14s.

- **2026-05-17** — 🎉 Hito 1.7 Demo cajero retail end-to-end + cierre Hito 1 completo: script CLI `apps/api/scripts/demo-cajero-retail.ts` ejecutable con `pnpm --filter @gaespos/api demo:retail` que ejecuta los 16 pasos del flujo retail contra una API live (login admin→tenant fresh→bootstrap dueño→cajero→config CFDI→catálogo+stock→apertura caja→búsqueda barcode→preview→cobro multi-pago→CFDI 4.0 timbrado UUID válido→ticket JSON con autofactura URL→movimientos caja→corte X→venta extra→corte Z con denominaciones y cuadre/sobrante/faltante→verificación 409 ventas post-Z bloqueadas). Salida colorizada con asserts en cada paso, dump del body + exit 1 si algo falla. Nuevo endpoint admin `POST /tenants/:slug/bootstrap-owner` (argon2 password + rol dueno preset) para crear primer dueño programáticamente. Flag `FISCAL_PROVIDER=mock` en `server.ts` para inyectar MockFacturamaClient en dev/demo sin requerir API key real (warning visible al arrancar). `tsconfig.json` incluye `scripts/**/*`. Verificación end-to-end OK en local. Suite estable: 146 tests API + 22 perm + 16 pricing + 18 db + 3 fiscal = 205 verdes. **Hito 1 cerrado**: 8 sub-tareas + 11 commits feature + 6 packages workspace (`@gaespos/db/permissions/pricing/fiscal/api` + apps Tauri `print-bridge` scaffold). Diferidos explícitos: 1.5.c autofactura pública + 1.6.b ESC/POS Rust (esperan dominio + hardware), Hito 2+ (apartados/CxC/devoluciones), V1.5 (pgvector, S3, particionamiento, bulk import Eleventa), Hito 0.10-0.12 (Hetzner+dominio+GitHub remote externos).

- **2026-05-17** — Hito 1.6.a Print Bridge contrato JSON + endpoints backend cerrado: nuevo módulo `apps/api/src/modules/tenant/tickets/` con `GET /t/ventas/:id/ticket` (TicketVenta JSON con emisor+venta+lineas+pagos+totales+cfdi opcional+autofactura opcional) y `GET /t/cortes/:id/ticket` (TicketCorte con arqueo, desglose, denominaciones). Scaffold `apps/print-bridge` Rust+Tauri 2: Cargo.toml con axum+tokio+serde, src/main.rs con HTTP server 127.0.0.1:9876, src/types.rs con structs Rust que matchean el contrato JSON del backend, README extenso. Excluido del workspace pnpm (`!apps/print-bridge`). Decisión: backend genera JSON estable, Print Bridge Rust lo renderiza ESC/POS (1.6.b cuando haya hardware). 5 tests integración tickets (venta sin/con CFDI, autofactura URL+expira, corte con denominaciones). Suite: 146 tests API + 22 perm + 16 pricing + 18 db + 3 fiscal = 205 verdes.

- **2026-05-17** — Hito 1.5 Modelo 4.19 CFDI 4.0 + Facturama cerrado (autofactura pública diferida a 1.5.c): paquete `@gaespos/fiscal` nuevo con `FiscalProvider` interface + `MockFacturamaClient` (determinista para tests) + `FacturamaClient` (HTTP real sandbox/prod con auth Basic, timeout 15s, descarga XML+PDF) + tipos exportados (UsoCfdi G01/G03/D01/P01/S01, FormaPagoSat 6 valores, MetodoPagoSat PUE/PPD, RegimenFiscalSat 12 regímenes, MotivoCancelacionSat 01-04, FiscalError). 3 tests unit del mock. Schema tenant 4.19 con `CfdiConfig` singleton (rfcEmisor unique, facturamaApiKey + ambiente, autofactura settings; **certificados .cer/.key NO almacenados** — Facturama panel los tiene) + `Cfdi` (1:1 con Venta vía Venta.cfdiId unique, serie+folio unique, folioFiscal UUID SAT unique, snapshot completo emisor+receptor, sellos SAT + cadenaOriginal, xml + pdfBase64 inline V1, estado [pendiente|vigente|cancelado|error], autofacturaToken para portal). Migration add_cfdi_config_emitidos cross-tenant. Permiso CFDI_CONFIGURAR agregado al catálogo. Plugin Fastify `fiscal` con `FiscalProviderFactory` decorador inyectable (default = FacturamaClient; tests = MockFacturamaClient). Service `cfdis/service.ts` con `emitirCfdi` (transacción lógica: crea CFDI pendiente → llama provider → vigente|error con errorMensaje), `cancelarCfdi` (valida vigente, motivo 01 requiere folioFiscalRelacionado), `nextFolio` (counter upsert+increment). Endpoints: GET/PUT `/t/cfdis/config` (NO expone apiKey raw, sólo `facturamaApiKeyConfigured: true`), POST `/t/ventas/:id/cfdi/emitir`, POST `/t/cfdis/:id/cancelar`, GET `/t/cfdis` lista paginada con filtros, GET detalle (sin xml/pdf inline), GET `/xml` y `/pdf` descarga con content-type correctos. 15 tests integración cubriendo: config sin configurar→404, emitir sin config→409, PUT crea→201, GET no expone apiKey, emisión OK con UUID válido, venta.cfdiId actualizada, doble CFDI→409, cajero sin CFDI_CONFIGURAR→403, XML/PDF descargables, cancelación con motivo 02→Cancelado, doble cancel→409, motivo 01 sin folioRelacion→400. Suite: 141 tests API + 22 permissions + 16 pricing + 18 db + 3 fiscal = 200 verdes.

- **2026-05-17** — Hito 1.4 Modelo 4.11 Cortes X/Z + arqueo MX cerrado: schema tenant con 3 modelos + 3 enums (`CajaApertura` con estado abierta/cerrada y cerradaForzosa flag; `CajaMovimiento` con 8 tipos entrada/salida; `Corte` tipo X|Z con numero per-apertura unique, denominaciones jsonb estructura billetes 1000/500/200/100/50/20 + monedas 20/10/5/2/1/0.50 MX). Service `cortes/service.ts` refactorizado a complejidad ≤15 con helpers `summarizarVentas`, `summarizarMovimientos`, `serializeDesglose`, `totalDenominaciones`. `calcularArqueo` acumulado desde apertura.createdAt (convención POS retail no diferencial). `crearCorte` calcula diferencia=contado−esperado (positivo=sobrante, negativo=faltante); Z cierra apertura, X no. **Regla integrada en `crearVenta`**: `requireAperturaAbierta` bloquea ventas si caja no tiene apertura activa (409). Endpoints `/t/cajas/:cajaId/aperturar`, `/t/cajas/:cajaId/apertura-actual`, `/t/caja-movimientos`, `/t/cortes` POST/GET con `buildCorteWhere` helper para filtros. Permisos: CAJA_ABRIR, CAJA_CERRAR para Z, CAJA_CERRAR_FORZOSO para cierre forzado. **Bug fix encontrado**: `nextCorteNumero` antes contaba por (aperturaId, tipo) pero unique es global (aperturaId, numero); fix: counter sin filtro de tipo. **Ajuste a Hito 1.3**: tests de ventas ahora abren caja en beforeAll. 17 tests integración nuevos (apertura, segunda apertura→409, GET actual, ventas con/sin apertura, movimientos entrada/salida, corte X acumulado correcto + no cierra, Z cierra + bloquea ventas + nueva apertura permite cobrar, denominación incorrecta marca faltante, corte sobre apertura cerrada→409, cajero sin CAJA_CERRAR_FORZOSO→403). Suite: 125 tests API + 22 permissions + 16 pricing + 18 db = 181 verdes.

- **2026-05-17** — Hito 1.3 Modelo 4.9 Ventas + multi-pago + tickets cerrado: schema tenant con 4 modelos + 3 enums (`Venta` con folio único per-sucursal + desglose subtotal/descuento/iva/ieps/total/totalCobrado/cambioDado + cfdiId nullable para Hito 1.5; `VentaLinea` con snapshot inmutable jsonb + descuentosAplicados paso-a-paso + iva/ieps desglosado; `VentaPago` 7 métodos enum + referencia/autorizacion/ultimosCuatro; `VentaFolioCounter` para counters atómicos via upsert+increment). Migration `add_sales_payments_tickets` cross-tenant. **Service `ventas/service.ts` refactorizado con complejidad cognitiva ≤15**: `crearVenta` → validarSucursalCaja + ejecutarPreviewSegura (wrapper de `calcularPreview` mapeando PreviewError→VentaError) + loadSnapshots + buildLineasCalculo con `calcularImpuestosLinea` (asume precios CON IVA y desglosa: `iva = total - total/(1+tasa/100)`) + totalesVenta + validarPagos (suma≥total + cambio requiere efectivo) + persistirVenta dentro de `$transaction` (nextFolio + descontarStockLineas reusando `aplicarAjuste` con tipo `ajuste_negativo` + insertarLineasYPagos). `cancelarVenta` reinserta stock con `ajuste_positivo`. Endpoints `/t/ventas` con `buildVentaWhere` helper para filtros, GET detalle full, POST cobro, POST `/:id/cancelar` (permisos VENTAS_CANCELAR). 15 tests integración end-to-end: cobro simple folio `SUC-PRINCIPAL-{000001}`, folio incrementa, stock descontado atómicamente, IVA desglosado (116→100base+16iva), multi-pago tarjeta+efectivo con cambio, pagos insuficientes→400, cambio sin efectivo→400, stock insuficiente→409 sin mutar, snapshot preserva nombre tras modificar producto, cajaId fuera de sucursal→400, cancelación devuelve stock, cancelar ya cancelada→409, cajero sin VENTAS_CANCELAR→403. Diferidos: CFDI emisión a 1.5 (cfdiId ya existe), HTML/CSS ticket a 1.6 Print Bridge, apartados/CxC/devoluciones parciales/IEPS detallado a Hito 2+. Suite: 108 tests API + 22 permissions + 16 pricing + 18 db = 164 verdes en ~12s.

- **2026-05-16** — Hito 1.2 Modelo 4.7 Productos + Inventario + Motor de precios cerrado: schema tenant con 20 modelos + 10 enums (Categoria árbol auto-ref, Marca, Producto con flags fiscales MX completos, ProductoVariante Shopify-style, ProductoCodigoBarras/Imagen/Atributo/Tag, InventarioSucursal DECIMAL 18,3 + stockReservado, InventarioMovimiento append-only, ProductoLote caducidad, ProductoSerie electrónicos, NivelPrecioMayoreo, ListaPrecio + ListaPrecioItem con precioMinimoNegociacion, ProductoPrecioEscalonado RF-02, ReglaPrecio con condicion/accion jsonb + join tables productos/categorias, CuponTenant). Diferidos a V1.5: pgvector, particionamiento, triggers postgres. Migration `add_products_inventory_pricing` generada con shadow schema postgres + aplicada a 3 tenants. **Paquete `@gaespos/pricing`** nuevo con motor cascada 6 pasos puro (Decimal.js) + 16 tests unit. Seed extendido crea lista PUBLICO default. **Endpoints `/t/{categorias,marcas,productos,variantes,inventario,inventario/ajustes,inventario/transferencias,inventario/movimientos,lotes,series,precios/listas,precios/escalonados,precios/reglas,precios/cupones,precios/preview}`** — POST `/precios/preview` ⭐ es el endpoint clave del POS: carga datos del tenant e invoca motor cascada. Lógica inventario en `service.ts` con `aplicarAjuste` y `aplicarTransferencia` atómicas (transaction Prisma + InsufficientStockError → 409). Plugin tenant-context extendido a `TenantPrincipal` con userId/email/tenantSlug del JWT. 52 tests integración nuevos (catálogo 23 + inventario 14 + precios 15). Total suite: 93 tests API + 22 permissions + 16 pricing + 18 db = 149 tests verdes en ~12s.

- **2026-05-15** — Hito 1.1 Modelo 4.6 Usuarios + Sucursales + Cajas + RBAC tenant: schema tenant Prisma con 7 modelos nuevos (`Sucursal`, `Caja`, `Usuario`, `UsuarioSucursal`, `Rol`, `UsuarioRol`, `UsuarioVistaGuardada`) + enums (`SucursalTipo`, `CajaTipo`, `UsuarioTipo`). Roles guardan permisos como `Json @default("[]")` (no tabla separada). Migration `add_users_branches_cashiers` aplicada cross-tenant. Nuevo paquete `@gaespos/permissions` con catálogo tipado de 56 permisos, helpers `hasPermission/requirePermission/mergeRolePermissions`, `PermissionDeniedError` con `missing[]`, `PRESET_ROLES_RETAIL` (6 roles: dueno wildcard, gerente, cajero, vendedor, almacen, contador_interno). Tenant client cache (LRU 50 default) con `createTenantClient/getTenantClient/disconnectAllTenantClients`. Seed defaults idempotente por tenant: 6 roles preset + sucursal `SUC-PRINCIPAL` + caja `CAJA-1`. CLI extendido: `gaes-migrate tenant seed <slug>` y `seed-all`. JWT con discriminador `kind: "admin"|"tenant"`; decoradores `authenticateAdmin/authenticateTenant` validan kind. Nuevo `POST /auth/tenant/login` y `GET /auth/tenant/me` con permisos efectivos. Plugin `tenantContextPlugin` decora `req.tenantPrisma/tenantSlug/principal/requirePerm()`. Rutas tenant montadas bajo `/t` prefix: `/t/sucursales`, `/t/cajas`, `/t/roles` (bloquea editar preset 403), `/t/usuarios` con asignación de roles/sucursales/reset-password. Error handler usa duck-typing para Prisma errors (cross-generator master/tenant). Helper `stripUndefined` para `exactOptionalPropertyTypes`. 21 tests integración nuevos (auth tenant + CRUD con gates de permisos owner vs cajero); total 41 verdes en 6.61s.

### Added
- **2026-04-27** — Estructura de continuidad inicial: `CLAUDE.md`, `STATUS.md`, `CHANGELOG.md`, `docs/hitos/`, `docs/adr/`, `docs/analisis/`, `docs/decisiones-pendientes.md`
- **2026-04-27** — Checklist Hito 0 (Infra base) en `docs/hitos/hito-0-infra.md`
- **2026-04-27** — Template ADR en `docs/adr/000-template.md`
- **2026-04-27** — Migración completa de los 10 análisis pre-código al repo (37 archivos en `docs/analisis/`): modelo de negocio, verticales y alcance, 10 flujos diarios, 18 sub-modelos de datos, reglas MX, hardware, integraciones, offline-first, arquitectura técnica, roadmap. Repo es ahora fuente de verdad versionada.
- **2026-04-27** — 12 ADRs derivados de Análisis 9 (`docs/adr/001-012`): multi-tenancy schema-per-tenant, Tauri sobre Electron, Print Bridge sobre WebUSB, Fastify sobre Express, TanStack Router sobre React Router, Hetzner+Coolify sobre AWS V1, Stripe Connect Direct sobre Platform MX, WhatsApp Cloud Meta directo sobre Twilio, SQLite + sync queue sobre CRDTs, PHR master DB sobre per-tenant, NO IA clínica decisional (línea roja), Biome sobre ESLint+Prettier.

### Closed (pre-código)
- **2026-04-27** — Análisis 1-10 cerrados al 100% (modelo negocio, verticales, flujos, modelo de datos, reglas MX, hardware, integraciones, offline-first, arquitectura, roadmap).

### Infra (Hito 0)
- **2026-04-28** — Hito 0.4 Setup monorepo completo: `pnpm@10.33.2` via Corepack, `git init -b main`, `package.json` workspace root (engines `node>=20.18`, `pnpm>=10`), `pnpm-workspace.yaml` (apps/*, packages/*, services/*), `turbo.json` con pipelines build/dev/lint/typecheck/test/clean, `tsconfig.base.json` TS strict total, `.nvmrc` (22), `.npmrc`, `.editorconfig`, `.gitignore`, `README.md` mínimo. Deps root: turbo 2.9.6 + typescript 5.9.3 + @types/node 22.19.17.
- **2026-04-28** — Hito 0.5 Lint + hooks: Biome 1.9.4 (linter + formatter), husky 9.1.7, commitlint 19.8.1 + config-conventional, lint-staged 15.5.2. `biome.json` con reglas TS strict (noExplicitAny, useImportType, noNonNullAssertion warn). `commitlint.config.cjs` extiende conventional. Hooks `.husky/pre-commit` (lint-staged + turbo typecheck) y `.husky/commit-msg` (commitlint). `tsconfig.json` raíz extiende base con project references vacías.
- **2026-04-28** — Hito 0.6 Package `@gaespos/db` + Docker dev: Prisma 6.19.3, Postgres 16-alpine + Redis 7-alpine via docker-compose (healthchecks, volúmenes persistentes, Redis mapeado a host 6380 por conflicto). Schema master con `Plan` (cuid, code, priceCents, currency MXN), `Tenant` (slug, schemaName único, status enum, planId FK), `AuditLog` cross-tenant. Tenant template Hito 0 vacía (modelos en Hito 1+). Factory `createMasterClient` + singleton HMR-safe. Seed con 4 planes (Free/Starter/Growth/Scale). Scripts root `dev:db*` y `db:*` via dotenv-cli. Verificación E2E: migration `init` aplicada, 4 tablas, 4 plans seedeados.
- **2026-04-28** — Hito 0.7 CLI `gaes-migrate` para migraciones multi-schema: commander 13 + pg 8 + execa 9. Comandos: `master`, `tenant create <slug> --name <n> --plan <code>`, `tenant migrate <slug>`, `tenant migrate-all`, `tenant list`. Reorganización `prisma/master/{schema, migrations}` + `prisma/tenant/{schema, migrations}` para historial separado por schema. Verificación E2E: 2 tenants creados (demo trial free, acme trial starter) con sus schemas postgres correspondientes (tenant_demo, tenant_acme); migrate-all idempotente.
- **2026-04-29** — Hito 0.8 App `@gaespos/api` Fastify 5 + JWT auth: schema master extendido con `AdminUser` (argon2 password hash, role enum, mfaSecret opcional) + `RefreshToken` (SHA-256 hash, expiresAt, revocación). Migration `add_admin_users`. Seed con admin default. Stack apps/api: Fastify 5.2 + @fastify/{cors,helmet,jwt,cookie,rate-limit,sensible} + Zod + Pino. Plugins db/auth/security/error-handler. Módulos health (`/health`, `/ready`), auth (`POST /auth/login` con rate-limit 10/min, `/refresh` con rotación, `/logout` con revocación, `GET /auth/me`), tenants (CRUD: list, get-by-slug, create — reusa `createTenant` de `@gaespos/db`). Refresh cookie HttpOnly Path=/auth signed Max-Age=30d. Access JWT 15min HS256. Verificación E2E completa: login → me → list/create tenants → refresh → logout → password mala 401.
- **2026-04-29** — Hito 0.9 CI GitHub Actions + tests integración Vitest: 38 tests verdes (18 unit packages/db utils + 20 integración apps/api auth/tenants/health con Postgres real, sin mocks). Vitest 2.1.9 con configs inline por package. Helpers test: buildTestApp con rate-limit relajado, loginAdmin, cleanupTestTenants con DROP SCHEMA CASCADE en slugs `test-*`. Tsconfig split build/typecheck para que tests no rompan rootDir. Workflows `.github/workflows/pr.yml` (services postgres+redis, install + biome ci + typecheck + test + build) y `main.yml` (placeholder deploy staging). Turbo `globalEnv` + `test.env` para invalidación correcta de cache. Cierra TODO de 0.7 (tests utils CLI).
