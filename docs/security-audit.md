# Bitácora de auditoría de seguridad

Registro de vulnerabilidades detectadas, decisión del panel de revisión y fix aplicado.
Formato por entrada: severidad · archivo · explotación · decisión del panel · fix aplicado.

---

## Ronda 1 — 2026-07-27

Rama: `seguridad/auditoria`. 5 vulnerabilidades aprobadas por el panel y aplicadas.

### R1-01 · HIGH · Owner account takeover vía reset-password (broken-authorization)

- **Archivo**: `apps/api/src/modules/tenant/usuarios/routes.ts` (`POST /t/usuarios/:id/reset-password`)
- **Explotación**: `POST /t/usuarios/:id/reset-password` solo validaba `PERMISSIONS.USUARIOS_RESET_PASSWORD` y sobrescribía el `passwordHash` de cualquier `:id` sin verificar el privilegio del objetivo. El preset `gerente` posee ese permiso. Un gerente (no-dueño) podía: (1) listar usuarios y ubicar al dueño (rol con permiso wildcard `*`), (2) resetear su contraseña a una conocida, (3) iniciar sesión como dueño con permisos totales (`["*"]`), incluyendo acceso a billing SaaS. El login de tenant autentica solo con `passwordHash`, y el 2FA forzado solo aplica a verticales de salud, por lo que era explotable hoy en retail/abarrotes/mayoreo.
- **Decisión del panel**: Ganadora propuesta 3 (consenso 3/3 en el enfoque). Validar el privilegio del OBJETIVO, no solo el permiso del actor: si el usuario objetivo es dueño y el actor NO es dueño (`req.principal.isOwner === false`), responder 403 antes de sobrescribir. Reutiliza el criterio de dueño existente (permiso `*`) sin inventar jerarquía nueva; mantiene `requirePerm` como puerta base (defensa en profundidad).
- **Fix aplicado**: En el handler `reset-password` se carga el objetivo con sus roles/permisos; si no existe → 404; se deriva `targetIsOwner` (algún rol incluye `OWNER_WILDCARD`); si `targetIsOwner && !req.principal.isOwner` → 403 "Solo un dueño puede restablecer la contraseña de otro dueño." Solo si pasa el guard se re-hashea y actualiza. Las vías hermanas de escalada ya estaban cerradas (`assertCanGrantRoles`, `assertCanDeactivateTarget`).

### R1-02 · HIGH · Proveedor de pago mock disponible en producción (payment-bypass)

- **Archivos**: `apps/api/src/plugins/pagos.ts`, `apps/api/src/modules/tenant/checkout/schemas.ts`, `apps/api/src/modules/tenant/checkout/routes.ts`
- **Explotación**: `defaultFactory` devolvía siempre un `MockPaymentProvider` real para el proveedor `mock`, sin gating de entorno. Además `mock` era el `.default()` de `proveedorPago` en ambos schemas de checkout. La firma del mock es trivialmente falsificable (`signature === mock-sig:${intentId}`) y `parseWebhook` devuelve `confirmado` para cualquier `intentId`. Las rutas `/t/checkout/webhook` y `/t/checkout/confirmar-mock` no exigen permiso (solo JWT de tenant). Cualquier usuario del tenant podía: iniciar checkout con `proveedorPago:'mock'`, confirmar-mock con el `intentId`, y marcar el pedido `pago_confirmado` → generar venta, descontar stock y emitir guía de envío SIN cobro real. Peor aún, por el default, pedidos sin `proveedorPago` explícito nunca se cobraban.
- **Decisión del panel**: Ganadora propuesta 1 (mayoría 2/3, defensa en profundidad). Gatear el mock en el chokepoint del factory con doble condición (`NODE_ENV !== 'production'` AND `PAGOS_ALLOW_MOCK === 'true'`), lanzando `PagoError('...','PROVIDER_UNAVAILABLE')`; quitar el `.default('mock')` de ambos schemas (proveedorPago requerido); usar `resolverProvider` en `/confirmar-mock` para que el rechazo salga como 503 controlado. Rechazada la variante que inyectaba el mock por env var sin chequeo de `NODE_ENV` (reactivable en prod).
- **Fix aplicado**: `pagos.ts` importa `PagoError` y define `mockAllowed()` (doble gate); la rama `mock` de `defaultFactory` lanza `PROVIDER_UNAVAILABLE` si el gate no pasa. `schemas.ts`: `proveedorPago: z.enum([...])` sin `.default("mock")` en `iniciarCheckoutSchema` y `webhookSchema`. `routes.ts`: `/confirmar-mock` usa `resolverProvider(app, "mock", reply)`; `handleErr` mapea `PROVIDER_UNAVAILABLE → 503`. En producción el factory rechaza el mock ANTES de tocar `procesarWebhookPago`/`crearVenta`. Stripe/Conekta siguen validando firma HMAC real.

### R1-03 · MEDIUM · Non-owner puede desactivar/archivar al dueño (broken-authorization / DoS)

- **Archivo**: `apps/api/src/modules/tenant/usuarios/routes.ts` (`DELETE /t/usuarios/:id`, `PATCH /t/usuarios/:id`)
- **Explotación**: `DELETE /:id` (USUARIOS_ARCHIVAR) y `PATCH /:id` con `{isActive:false}` (USUARIOS_ACTUALIZAR) ponían `isActive=false` sobre cualquier objetivo sin chequeo de privilegio. El preset `gerente` posee ambos permisos. El dueño es una fila `usuario` con el rol `dueno`, por lo que era objetivo válido; el login rechaza usuarios inactivos → lockout duro. Un gerente podía desactivar al dueño (y a todo admin), dejándolos fuera del tenant.
- **Decisión del panel**: Ganadora propuesta 1 (consenso 3/3). Guard de autorización a nivel de objetivo: un actor no-dueño no puede desactivar/archivar a un usuario que porta el rol dueño (permiso `*`). Reutiliza la detección de dueño existente. Capa aditiva; `requirePerm` sigue validando el permiso base.
- **Fix aplicado**: Helpers `isTargetOwner(req, targetId)` (target inexistente → false) y `assertCanDeactivateTarget(req, targetId)` (early-return si `req.principal.isOwner`; si el target es dueño lanza `PermissionDeniedError([USUARIOS_ARCHIVAR])` → 403). `DELETE /:id` llama el guard antes del update. `PATCH /:id` lo llama solo cuando `body.isActive === false` (reactivaciones y ediciones normales no se bloquean ni pagan query extra). Los admins no-dueño desactivados siguen siendo reactivables por el dueño → severidad medium.

### R1-04 · MEDIUM · Challenge WebAuthn stateless nunca consumido → replay de aserción (auth-webauthn)

- **Archivos**: `apps/api/src/modules/auth-tenant/passkey-routes.ts`, `packages/db/prisma/master/schema.prisma`, `packages/db/prisma/master/migrations/20260727000000_add_webauthn_used_challenges/`
- **Explotación**: El challenge de passkey era un token HMAC self-contained sin almacenamiento server-side; `verificarChallenge` solo validaba HMAC + expiración de 5 min. En `/passkey/login/verify` el challenge nunca se marcaba usado tras un login exitoso, así que el mismo `{challengeToken, response}` se aceptaba repetidamente durante los 5 min. Las passkeys de plataforma (Apple/Google/Windows Hello) reportan `signCount=0`, por lo que la detección de clones de simplewebauthn (`newCounter > storedCounter`) nunca dispara → replays ilimitados, cada uno acuñando un JWT de tenant fresco (15 min). Capturando un solo POST legítimo (extensión maliciosa, XSS, proxy con logging) un atacante obtenía N JWTs válidos.
- **Decisión del panel**: Ganadora propuesta 3 (mayoría unánime). Mantener el challenge HMAC stateless, añadir un `jti` UUID al `ChallengePayload` (firmado, inmanipulable) y consumirlo atómicamente vía `INSERT` con PK única en la tabla master `webauthn_used_challenges`; el replay choca con P2002 → 401. Orden seguro: consumir DESPUÉS de que `verifyAuthenticationResponse` valida y ANTES de acuñar el JWT/actualizar counter (una aserción inválida no quema el challenge). Race-safe: dos replays concurrentes compiten en el INSERT atómico. Protege también `register/verify`.
- **Fix aplicado**: Nueva tabla master `WebauthnUsedChallenge` (`id` = jti PK, `expiresAt`, `createdAt`, índice en `expires_at`) + migración `20260727000000_add_webauthn_used_challenges`. `passkeyRoutes`: `ChallengePayload` incluye `jti: string`; ambas opciones (register/login) generan `jti: randomUUID()`; helper `consumirChallenge(masterPrisma, jti, exp)` hace `create` y narrowea P2002 → false. `login/verify`: tras `verified` y antes del update de counter/JWT, `if (!await consumirChallenge(...)) return unauthorized(reply)`. `register/verify`: tras `verifyRegistrationResponse` OK y antes del `create` de la credencial, consume o responde 400. **Pendiente operativo obligatorio antes de desplegar**: ejecutar `prisma migrate deploy` en producción para crear la tabla antes de subir el código.

### R1-05 · MEDIUM · Sync /push bypassa `ventaCreateSchema` y el gate RBAC `ventas.crear` (input-validation / broken-authorization)

- **Archivos**: `apps/api/src/modules/tenant/sync/service.ts`, `apps/api/src/modules/tenant/sync/routes.ts`
- **Explotación**: `POST /t/:slug/sync/push` estaba gateado solo por `SYNC_USAR`. El payload de cada operación es `z.record(z.string(), z.unknown())` (JSON arbitrario) y para `entityType === 'venta'` se pasaba crudo a `crearVenta(prisma, userId, op.payload as never)` — la ÚNICA vía de creación de venta sin `ventaCreateSchema` ni el permiso `VENTAS_CREAR`. Un tenant podía acuñar un rol custom con `sync.usar` pero sin `ventas.crear` y escalar a creación de ventas `cobrada` (descuenta stock, genera corte, debita monedero/CxC del cliente). Además, un `pago.monto` no numérico llegaba a `new Decimal()` → 500 no controlado.
- **Decisión del panel**: Ganadora propuesta 2 (mayoría 2/3, defensa en profundidad). Doble capa: (a) gate por operación en `aplicarVenta` (`VENTAS_CREAR` + validación con `ventaCreateSchema` + eliminar `as never` + paridad `permiteDescuentoAlto`) — barrera definitiva que conserva la semántica de éxito parcial; (b) gate condicional a nivel de ruta que exige `VENTAS_CREAR` con 403 temprano (fail-fast) cuando el batch contiene alguna op `venta`, alineado con la ruta directa `POST /ventas`.
- **Fix aplicado**: `service.ts` (ya implementado): `aplicarVenta(prisma, principal, userId, op)` retorna `failed` si `!hasPermission(principal, VENTAS_CREAR)`; valida con `ventaCreateSchema.safeParse` (sin `as never`); pasa `{ permiteDescuentoAlto }` a `crearVenta`. `procesarPush` propaga `principal`. `routes.ts` (aplicado en esta ronda): tras `requirePerm(SYNC_USAR)`, si `body.operations.some(op => op.entityType === "venta")` → `req.requirePerm(PERMISSIONS.VENTAS_CREAR)` (fail-fast 403). Resultado: autorización cerrada en dos capas, validación con el esquema canónico (elimina el 500 por monto no-numérico) y paridad de descuento con la ruta directa.

**Verificación**: `pnpm --filter @gaespos/api exec tsc -p tsconfig.json --noEmit` → 0 errores.

---

## Ronda 1 (refuerzo) — 2026-07-28

Rama: `seguridad/auditoria`. El panel emitió el consenso definitivo (`fixConsensuado`) para las 5 vulnerabilidades de Ronda 1. Al contrastarlo con lo ya aplicado en `7af0b89`, tres fixes requerían endurecimiento adicional que cierra residuales reales (escalada lateral, reapertura por mala config, y un bypass del lockout). R1-04 y R1-05 ya estaban completos y se verificaron sin cambios.

### R1-01 · HIGH · Reset-password: cerrar también la escalada lateral (broken-authorization)

- **Archivo**: `apps/api/src/modules/tenant/usuarios/routes.ts` (`POST /t/usuarios/:id/reset-password`)
- **Explotación residual**: El guard aplicado en `7af0b89` (`targetIsOwner && !isOwner`) bloqueaba solo el reset del dueño. Un `gerente` seguía pudiendo resetear la contraseña de un admin de MAYOR privilegio no-dueño (p.ej. alguien con permisos que el gerente no posee), tomando esa cuenta — escalada lateral gerente → admin-superior.
- **Decisión del panel**: Ganadora propuesta 1 (mayoría 2/3, patrón inline-403). Sustituir el guard owner-only por la MISMA comparación de superset de permisos que ya protege `assertCanGrantRoles`: un actor no-dueño no puede resetear a un objetivo que porte el wildcard `*` NI ningún permiso que el actor no tenga. Reusa el `target` ya consultado (sin query extra), conserva 404/204 y el Zod.
- **Fix aplicado**: En el handler, reemplazado el bloque owner-only por: si `!req.principal.isOwner`, construir `actorPerms = new Set(req.principal.permissions)` y calcular `targetOutranksActor` (algún rol del target incluye `OWNER_WILDCARD` o algún permiso `!actorPerms.has(p)`) → 403 "No puedes restablecer la contraseña de un usuario con mayor privilegio." El dueño sigue pudiendo resetear a cualquiera; un no-dueño solo a privilegio menor o igual. Cierra owner-takeover y escalada lateral, unificando el criterio con `assertCanGrantRoles`.

### R1-02 · HIGH · Pago mock: fail-fast de arranque + verificación de monto (payment-bypass)

- **Archivos**: `apps/api/src/plugins/pagos.ts`, `apps/api/src/modules/tenant/checkout/service.ts`
- **Explotación residual**: El bypass principal ya está cerrado (gate `mockAllowed()` + `proveedorPago` requerido). Quedaban dos residuales: (1) reapertura por mala config operativa (`PAGOS_ALLOW_MOCK=true` en un box de prod), donde `simularWebhook` reusa el monto real del intent y una simple guarda de monto NO lo detendría; (2) ausencia de verificación de que el monto confirmado coincide con el total del pedido (defensa en profundidad para proveedores reales, recomendada por Stripe).
- **Decisión del panel**: Ganadora propuesta 1 como principal (fail-fast boot guard, sella la única vía de reapertura) + propuesta 3 como capa complementaria (verificación de monto). NO añadir `requirePerm` a `/webhook` ni `/confirmar-mock` (la autorización del callback es la firma del proveedor, no RBAC). NO tocar el frontend.
- **Fix aplicado**: `pagos.ts` — dentro de `pagosPlugin`, ANTES de `app.decorate`, `if (NODE_ENV === "production" && PAGOS_ALLOW_MOCK === "true") throw new Error("PAGOS_ALLOW_MOCK=true no permitido en producción: sería bypass de pago")`: convierte la mala config en fallo de arranque ruidoso en vez de cobro-cero silencioso (no afecta tests con `NODE_ENV=test` ni dev/BFF). `checkout/service.ts` — en `procesarWebhookPago`, tras el manejo de `status !== "confirmado"` y antes de leer items/`crearVenta`: `totalCentavos = Math.round(new Decimal(pedido.total.toString()).times(100).toNumber())`; si `evento.montoCentavos !== totalCentavos` → `CheckoutError(422, ...)` con `esperadoCentavos`/`recibidoCentavos`. Coincide por construcción con `iniciarCheckout` (`montoCentavos = total.times(100)`), así que no rompe webhooks legítimos (incluido MSI, cargo = total) y rechaza montos parciales/falsificados (`montoCentavos:1`) para TODOS los proveedores. **Operativo**: producción DEBE correr con `NODE_ENV=production` y NUNCA `PAGOS_ALLOW_MOCK=true` (ahora reforzado por el fail-fast).

### R1-03 · MEDIUM · Lockout del dueño vía remoción de rol (broken-authorization / DoS)

- **Archivo**: `apps/api/src/modules/tenant/usuarios/routes.ts` (`DELETE /t/usuarios/:id/roles/:rolId`)
- **Explotación residual**: El vector directo (PATCH/DELETE `:id` → `isActive=false`) ya lo cierra `assertCanDeactivateTarget`. Pero seguía abierto un bypass: `DELETE /:id/roles/:rolId` solo validaba `USUARIOS_ASIGNAR_ROL` sin guard de objetivo (`assertCanGrantRoles` protege solo el OTORGAR, no el REMOVER). El preset `gerente` posee `USUARIOS_ASIGNAR_ROL`. Cadena: gerente hace `DELETE /:id/roles/<rolId 'dueno'>` quitándole al dueño el rol portador de `*`; con ello `isTargetOwner(dueño)` pasa a `false`; acto seguido `DELETE /:id` o `PATCH {isActive:false}` ya no dispara el guard → dueño desactivado → login rechaza inactivos → lockout de la cuenta de mayor privilegio.
- **Decisión del panel**: Ganadora propuesta 1 (única correcta; verificó que la "mayoría" que declaraba "sin cambios" era técnicamente errónea por no auditar la remoción de rol). Añadir en `DELETE /:id/roles/:rolId` el mismo guard de objetivo-dueño: un no-dueño no puede quitar NINGÚN rol a un objetivo que es dueño. Reusa `isTargetOwner` y `PermissionDeniedError`/`PERMISSIONS` ya importados; sin Zod ni migraciones.
- **Fix aplicado**: En el handler, tras validar `rolId` y antes del `deleteMany`: `if (!req.principal.isOwner && (await isTargetOwner(req, params.id))) throw new PermissionDeniedError([PERMISSIONS.USUARIOS_ASIGNAR_ROL]);` → 403. El dueño (`isOwner`) sigue gestionando roles libremente. Con esto un no-dueño ya no puede ni desactivar (R1-03 vigente) ni demotar (este fix) al dueño, cerrando de forma real el lockout. Nota de alcance (deliberadamente fuera de este ticket): la desactivación lateral admin-vs-admin bajo `usuarios.archivar` sigue permitida; endurecerla exigiría comparar permisos actor↔objetivo, no necesario para este reporte medium.

### R1-04 · MEDIUM · Replay WebAuthn — verificado sin cambios

- El fix single-use (jti + tabla master `webauthn_used_challenges`, `consumirChallenge` con INSERT atómico P2002, consumo tras verificación y antes del JWT) ya está completo en `passkey-routes.ts` + `schema.prisma` + migración `20260727000000`. **Pendiente operativo obligatorio**: aplicar la migración en la master DB de producción (`prisma migrate deploy`) antes o junto con el deploy; sin la tabla, `consumirChallenge` lanza P2021 en cada login por passkey.

### R1-05 · MEDIUM · Sync /push bypass — verificado sin cambios

- El fix de doble capa (gate por operación `VENTAS_CREAR` + `ventaCreateSchema.safeParse` en `aplicarVenta`, y fail-fast 403 a nivel de ruta cuando el batch trae `venta`) ya está completo en `sync/service.ts` + `sync/routes.ts`. Sin cambios en esta ronda.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores.

---

## Ronda 2 — 2026-07-28

Rama: `seguridad/auditoria`. 5 vulnerabilidades aprobadas por el panel y aplicadas.

### R2-01 · CRITICAL · Admin no-superadmin (support/billing) puede tomar cualquier tenant vía `/tenants` (privilege-escalation)

- **Archivo**: `apps/api/src/modules/tenants/routes.ts`
- **Explotación**: El plugin `tenantRoutes` (montado en `/tenants`) estaba protegido solo por `app.authenticateAdmin`, que valida `req.user.kind === "admin"` pero NO `req.user.role`. Las cuentas admin tienen roles `superadmin`/`support`/`billing` y el login mintea un JWT `kind:"admin"` con el rol que sea. Los módulos hermanos (`admin/tenants-routes.ts`) sí gatean las MISMAS operaciones create/estado/plan con `requireSuperadmin`, pero este módulo omitía el check en todas sus rutas mutantes: `POST /` (create), `/:slug/suspender`, `/reactivar`, `/cancelar`, `/cambiar-plan`, `/aplicar-cupon` y el crítico `/:slug/bootstrap-owner`. Un admin `support`/`billing` podía `POST /:slug/bootstrap-owner` con email/password propios para crear un usuario con rol `dueno` (permisos `*`) dentro de CUALQUIER tenant y luego loguearse con control total; o `POST /:slug/cancelar` para destruir la suscripción de cualquier cliente.
- **Decisión del panel**: Ganadora propuesta 1 (mayoría 2/3, enfoque fail-safe por método HTTP). Añadir un segundo `preHandler` a nivel de plugin: `GET` libre para cualquier admin; todo lo no-GET exige `role === "superadmin"`. Superior al guard repetido por handler porque es DRY y protege automáticamente cualquier ruta mutante futura (el olvido que causó la vulnerabilidad). Todas las rutas mutantes son POST y las de lectura son GET, así que el filtro cubre exactamente las 7 críticas dejando los GET accesibles a support/billing (consistente con `admin/tenants-routes.ts`).
- **Fix aplicado**: Tras `app.addHook("preHandler", app.authenticateAdmin)` se añadió un segundo `preHandler`: si `req.method === "GET"` retorna; si `req.user.kind === "admin" && req.user.role === "superadmin"` retorna; en caso contrario 403 "Solo un superadmin puede administrar clientes". El narrowing `kind === "admin"` tipa `role` sin `any` (TS strict). support/billing reciben 403 antes de ejecutar cualquier escritura; GET `/` y GET `/:slug` siguen accesibles.

### R2-02 · HIGH · Secuestro de subdominio de tienda cross-tenant (multi-tenant-isolation)

- **Archivos**: `apps/api/src/modules/tenant/ecommerce-config/dominio-service.ts`, `apps/api/src/modules/tenant/ecommerce-config/routes.ts`
- **Explotación**: `sincronizarDominioMaster()` hacía un `upsert` incondicional sobre la tabla GLOBAL master `tiendaDominio` (clave `host`, namespace compartido por todos los tenants) cada vez que un tenant guardaba su config de e-commerce, reescribiendo `tenantSlug` al del llamador y forzando `verificado:true`. El `host` deriva de `body.subdominio` con solo validación de formato y SIN check de propiedad cross-tenant. La ruta hermana `b2b-dominio` sí implementa la guarda (findUnique + `if existente.tenantSlug !== req.tenantSlug return 409`) pero aquí faltaba. Un tenant B con e-commerce podía `PUT /t/ecommerce/config` con `{subdominio:'acme'}` (subdominio de la víctima A) y `resolverTenantPorHost('acme.<apex>')` pasaba a devolver B → robo de tráfico del storefront de A. La rama de dominio propio permitía además tumbar el dominio verificado de A (flip a `verificado:false` → DoS).
- **Decisión del panel**: Ganadora propuesta 1 (mayoría; superset del consenso). Replicar la guarda cross-tenant de `b2b-dominio` en DOS puntos: pre-chequeo en la ruta (ANTES de escribir `configTiendaEcommerce`, para no dejar escritura parcial) + guarda justo antes de cada `upsert` dentro de `sincronizarDominioMaster`; más un `setErrorHandler` a nivel de plugin que mapea `DominioEnUsoError` a 409 (DRY, cubre `PUT /config` y `POST /dominio/verificar` y cierra la carrera pre-check→upsert).
- **Fix aplicado**: `dominio-service.ts` — nueva clase exportada `DominioEnUsoError`; helper privado `asegurarHostDisponible(master, tenantSlug, host)` (findUnique en `tiendaDominio`; si `existente.tenantSlug !== tenantSlug` lanza; mismo dueño = idempotente); wrapper exportado `asegurarHostsDisponibles` que deriva subdominio (solo si hay `STOREFRONT_APEX`) y dominio propio en minúsculas; invocación de la guarda inmediatamente ANTES de cada `upsert` (rama subdominio y rama propio). `routes.ts` — import de `asegurarHostsDisponibles` y `DominioEnUsoError`; `setErrorHandler` del plugin → 409; en `PUT /config`, tras leer `existing` y antes de escribir, pre-chequeo con el `dominioPropio` efectivo (undefined = conservar existente). Usa `app.masterPrisma` para la tabla global y `req.tenantPrisma` para la config. Sin tocar `schemas.ts` ni Prisma.

### R2-03 · HIGH · Alta de passkey sin re-autenticación step-up → takeover permanente desde token de 15 min (auth-persistence)

- **Archivo**: `apps/api/src/modules/auth-tenant/passkey-routes.ts`
- **Explotación**: `POST /passkey/register/options` y `/verify` estaban gateados solo por `authenticateTenant` (JWT válido), añadiendo una credencial de autenticación nueva SIN pedir contraseña ni TOTP (a diferencia de `/mfa/disable` que revalida password y `/mfa/backup-codes/regenerate` que revalida TOTP). Como el SPA persiste el access token, un token de 15 min robado (XSS / terminal compartida) podía convertirse en un autenticador permanente que sobrevive al reset de contraseña y evade el mandato 2FA, dando acceso passwordless indefinido.
- **Decisión del panel**: Ganadora propuesta 3 (mayoría 2/3 en exigir password SIEMPRE + TOTP si el usuario tiene MFA activo; elegida sobre la 1 por alinearse mejor al codebase, menor riesgo de regresión). Gatear SOLO `POST /passkey/register/options` (el `/verify` queda protegido transitivamente: requiere un `challengeToken` HMAC firmado, ligado a usuarioId/tenantSlug, purpose "reg", TTL y consumo single-use que solo `/options` emite). Usa el helper existente `loadTenantUserMfa` y condiciona TOTP a `mfaEnabled && mfaSecret && mfaVerifiedAt`.
- **Fix aplicado**: Imports de `verifyPassword`, `verifyTenantTotp`, `loadTenantUserMfa` (y `require2faParaUsuario`). Nuevo `regOptionsSchema = z.object({ password: z.string().min(1), totp: z.string().optional() })`. En el handler de `/register/options`, antes de generar opciones: valida password contra `usuario.passwordHash` del tenant (`getTenantClient`) → 401 "Contraseña incorrecta"; si el usuario tiene MFA enrolado (`mfaEnabled && mfaSecret && mfaVerifiedAt`) exige TOTP válido → 401 "Código incorrecto". Además se endureció `userVerification: "required"` en registro y login, y `/passkey/login/verify` ahora reta segundo factor (`retarSegundoFactor` → token intermedio `kind:"tenant_mfa"`) cuando la política del tenant exige 2FA y la aserción no fue verificada, cerrando el bypass de 2FA en login por passkey. Sin `any`; passwordHash/MFA en DB del tenant, `webauthnCredential` en master. **Contrato frontend**: la pantalla "Agregar passkey" debe enviar `{ password, totp? }` en el body de `POST /auth-tenant/passkey/register/options`.

### R2-04 · HIGH · `condicion` JSON sin validar en reglas de precio tumba el motor (input-validation / DoS del POS)

- **Archivos**: `apps/api/src/modules/tenant/listas-precios/schemas.ts` (primario), `packages/pricing/src/calculate.ts` (defensa en profundidad)
- **Explotación**: `reglaCreateSchema`/`reglaUpdateSchema` tipaban `condicion` como `z.record(z.string(), z.unknown())`, sin validar la forma de los valores. El motor de pricing alimenta esos valores directo a `decimal.js` sin guarda: `new Decimal(condicion.montoMinimo)` lanza `DecimalError` con cualquier valor no numérico. `aplicarMayoreoTotalTicket` corre su filtro `montoMinimo` en TODO ticket, así que una sola regla `mayoreo_por_total_ticket` con `montoMinimo:"lol"` hace fallar `calcularTicket` en cada venta → 500 en `previewVenta` y `crearVenta`. El preset `gerente` (permiso `PRECIOS_REGLA_CREAR`) podía brickear el POS de todo el tenant con una request, de forma persistente (la regla queda `isActive=true` en DB).
- **Decisión del panel**: Ganadora propuesta 1 (consenso 3/3 en la raíz + única que aborda la corrección total). Capa 1 (primaria): reemplazar el `z.record` permisivo por un `condicionSchema` tipado con `.strict()` que espeja `ReglaCondicion`, reusando el helper `positiveDecimalString` para `montoMinimo`/`cantidadMinima`. Capa 2a (requerida, defensa en profundidad): guard en el load-path del motor para que una regla envenenada residual (persistida antes del parche) nunca vuelva a lanzar. Capa 2b (operativa): SQL de limpieza de filas envenenadas.
- **Fix aplicado**: `schemas.ts` — `condicionSchema` (`.strict()`) con `productosAplicables`/`categoriasAplicables`/`clientesAplicables` como `z.array(z.string()).optional()`, `cantidadMinima`/`montoMinimo` como `positiveDecimalString.optional()`, `bogo*` como enteros positivos; usado en create (`.default({})`) y update (`.optional()`). `positiveDecimalString` solo acepta `number>0` o string `/^\d+(\.\d+)?$/`, así que `"lol"`/objetos/arrays se rechazan con 4xx antes de persistir. `calculate.ts` — helper `decOrNull(v)` que devuelve `null` en vez de lanzar ante valores no finitos/no parseables; usado en `aplicarMayoreoTotalTicket` (`min === null || subtotal.gte(min)`) y `reglaAplica` (`cantMin !== null && ...`), de modo que una regla residual inválida se ignora en vez de tumbar el motor. **Operativo (Capa 2b)**: en entornos que pudieron recibir el exploit, localizar/desactivar reglas activas con `condicion.montoMinimo`/`cantidadMinima` no numéricas por tenant (`SELECT ... FROM "ReglaPrecio" WHERE "isActive"=true AND (condicion->>'montoMinimo') !~ '^\d+(\.\d+)?$' ...`). No se tocó Prisma.

### R2-05 · HIGH · SSRF autenticado en `apiUrl` de proveedor de recargas (ssrf)

- **Archivos**: `packages/recargas/src/ssrf-guard.ts` (nuevo), `packages/recargas/src/recargaki.ts`, `packages/recargas/src/index.ts`, `apps/api/src/modules/tenant/recargas/schemas.ts`
- **Explotación**: `apiUrl` de la config de proveedor se validaba solo con `z.string().url()` sin allowlist de host/esquema/IP. Un usuario con `RECARGAS_CONFIGURAR` lo guardaba y, al vender (`RECARGAS_VENDER`), el server hacía `fetch(\`${apiUrl}/recharge\`)` server-side. Sin filtro de destino, el server multi-tenant podía ser forzado a golpear la red interna o el endpoint de metadatos cloud (`169.254.169.254`, `*.railway.internal`), con exfiltración parcial de respuestas internas reflejadas en el mensaje de error `RECARGAKI_HTTP_*`.
- **Decisión del panel**: Ganadora propuesta 2 (superset del consenso; cierra el TOCTOU de DNS rebinding sin dependencias nuevas vía host-allowlist), endurecida con los rangos IP más completos de la propuesta 1. Defensa en profundidad en dos puntos: refine Zod síncrono en el `PUT` de config + `assertSsrfSafeUrl` con resolución DNS previa al `fetch`; más eliminación de la reflexión del cuerpo interno y `redirect:"error"`.
- **Fix aplicado**: Nuevo `ssrf-guard.ts` con `isSsrfSafeUrlSync` (https-only + denylist de rangos privados/reservados IPv4/IPv6 + `hostAllowlist()` derivado de `PROVEEDORES_V1` u override `RECARGAS_HOST_ALLOWLIST`) y `assertSsrfSafeUrl` (repite el check síncrono + `lookup(host,{all:true})` que bloquea si CUALQUIER dirección resuelta es privada o si no resuelve). Denylist endurecida en esta ronda: `0/8`, `10/8`, `127/8`, `169.254/16` (incl. metadata), `172.16-31`, `192.168/16`, `100.64/10`, más `192.0.0/24`, `198.18-19` (benchmarking), `>=224` (multicast/broadcast), sufijo `.home.arpa`, y IPv6 `fe80::/10` vía regex `/^fe[89ab]/` con normalización de zona (`split("%")`). `recargaki.ts` — `await assertSsrfSafeUrl(this.cfg.apiUrl)` como primera línea de `callApi`, `fetch` con `redirect:"error"`, y mensaje de error sin reflexión (`RecargaKi ${op} retornó ${status}`). `index.ts` exporta ambos guards; `recargas/schemas.ts` aplica `.refine(isSsrfSafeUrlSync, ...)` a `apiUrl`. **Verificación**: `tsc --noEmit` limpio en `@gaespos/recargas` + `vitest` 17/17.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit`, `--filter @gaespos/pricing` y `--filter @gaespos/recargas` → 0 errores; `pnpm --filter @gaespos/recargas test` → 17/17.

---

## Ronda 3 — 2026-07-28

Rama: `seguridad/auditoria`. 3 vulnerabilidades aprobadas por el panel y aplicadas.

### R3-01 · HIGH · Un tenant se auto-otorga consentimiento PHR y lee el expediente cross-tenant de otra clínica (tenant-isolation)

- **Archivos**: `apps/api/src/modules/patient-portal/service.ts`, `apps/api/src/modules/patient-portal/schemas.ts`, `packages/db/prisma/master/schema.prisma`, migración `20260728000000_add_consent_granted_via`
- **Explotación**: El expediente maestro (PHR) es un recurso global en la master DB cuyos `patientRecord` los escriben muchos tenants (cada uno con su `tenantId`). La única frontera que impedía que el tenant A leyera las notas clínicas del tenant B sobre un paciente compartido era una fila `patientConsent`. Ese consent lo podía crear el propio tenant sin verificación del paciente: `registrarConsentimientoTenant` (vía `POST /t/consentimientos`, gateado solo por `PHR_SOLICITAR_CONSENT` que el rol preset `medico` posee) aceptaba un `patientId` arbitrario y cualquier `scope` del body (incluido `full_phr`), verificaba solo que el paciente existiera y escribía un consent activo. Luego `leerExpedienteTenant` consultaba `master.patientRecord.findMany({ where: { patientId } })` SIN filtro de `tenantId` (y sin `isVisibleToPatient`), así que un consent `full_phr` (cubreTodo) devolvía TODOS los registros del paciente escritos por CUALQUIER otra clínica, incluyendo notas ocultas al paciente. El mismo consent auto-otorgado habilitaba además ESCRIBIR registros forjados vía `publicarRegistro`. El tenant descubre `pacienteMasterId` gratis: cualquier paciente que reserva por el marketplace aparece en `GET /t/reservas` exponiendo `pacienteMasterId`, `pacienteEmail`, `pacienteNombre`.
- **Decisión del panel**: Ganadora propuesta 1 (mayoría 2/3; #1 y #2 son el mismo enfoque `grantedVia`, #2 construye sobre el schema de #1). Discriminador de procedencia `ConsentGrantedVia { patient_portal | tenant_attested }`: SOLO los consents `patient_portal` (otorgados por el paciente autenticado vía `otorgarConsent`) desbloquean lectura cross-tenant; los `tenant_attested` (auto-otorgados por la clínica) quedan limitados a los registros propios. Elegido sobre la #3 (`confirmedAt`) porque ésta convertía el path atestiguado en un no-op pendiente (una clínica que ve al paciente en persona no podría publicar hasta que el paciente entre al portal y confirme = regresión funcional alta). Se incorpora el hardening de escritura de la #2 (excluir `full_phr` del consent atestiguado).
- **Fix aplicado**:
  - **schema.prisma** (`~L551`, `~L716`): `enum ConsentGrantedVia { patient_portal tenant_attested }` y `grantedVia ConsentGrantedVia @default(patient_portal) @map("granted_via")` en `PatientConsent`. Migración `20260728000000_add_consent_granted_via/migration.sql`: `CREATE TYPE`; `ADD COLUMN NOT NULL DEFAULT 'tenant_attested'` (backfill seguro de consents viejos a deny cross-tenant); luego `ALTER COLUMN SET DEFAULT 'patient_portal'` para inserts futuros. Cliente Prisma master regenerado.
  - **service.ts**: `otorgarConsent` (ruta paciente autenticado) fija `grantedVia:"patient_portal"`; `registrarConsentimientoTenant` (ruta clínica) fija `grantedVia:"tenant_attested"`. `leerExpedienteTenant` deriva `cubreTodo`/`tiposPermitidos` SOLO de `consents.filter(c => c.grantedVia === "patient_portal")`; el `where` usa `OR` de dos ramas: propia `{ tenantId }` (siempre legible) y cross-tenant SOLO si hay consent `patient_portal` que la cubra, con `isVisibleToPatient:true` obligatorio (y `resourceType:{in:tiposPermitidos}` si no es `full_phr`). Sin consent del paciente = solo registros propios. Cierra la exfiltración cross-tenant y la fuga adicional de notas ocultas de otras clínicas.
  - **schemas.ts** (hardening de escritura, propuesta #2): `registrarConsentTenantSchema.scope` restringido a `z.enum(["appointments_only","prescriptions_only","vaccines_only"])`, EXCLUYENDO `full_phr`. Un tenant ya no puede auto-otorgarse `full_phr` ni publicar cualquier `resourceType`; queda acotado a los tipos que su consent presencial declara.
- **Fuera de alcance (ADR/producto, no en este parche)**: el cierre total del vector de ESCRITURA forjada vía `publicarRegistro` con consent `tenant_attested` exige verificación patient-side (OTP/firma) que rompería la publicación presencial legítima; se registra como decisión de producto, no de aislamiento. Cada registro lleva el `tenantId` autor auto-atribuido y auditado.

### R3-02 · HIGH · Doble canje de gift-card por `canje` no transaccional (race-condition / TOCTOU)

- **Archivo**: `apps/api/src/modules/tenant/monedero/service.ts`
- **Explotación**: `canjearGiftCardAMonedero` leía la tarjeta (`findUnique`), comprobaba `status !== "activa"`, acreditaba el monedero del cliente (`moverMonedero`) y SOLO DESPUÉS marcaba la tarjeta `agotada`/saldo 0. Tres round-trips independientes SIN transacción ni row-lock que abarque el check-and-consume; el flip a `agotada` era un blind write, no un compare-and-swap. N `POST /gift-cards/canjear` concurrentes para el mismo `codigo` pasaban todos el check `activa` antes de que ninguno marcara `agotada`, acreditando el saldo N veces mientras la tarjeta se consume una sola vez (una tarjeta de $1,000 disparada 5 veces → $5,000 en el monedero, gastable en POS). El módulo hermano `devoluciones` ya cerraba este mismo TOCTOU con `pg_advisory_xact_lock`; monedero no tenía equivalente.
- **Decisión del panel**: Ganadora propuesta 1 (consenso 3/3 en el enfoque). Convertir el check-and-consume en un consumo atómico compare-and-swap (`updateMany` con `where status='activa'`) dentro de un único `client.$transaction`, con el abono en la MISMA tx vía `aplicarMovimientoTx`. El CAS es superior al advisory lock para este caso porque el invariante a proteger es el `status` de UNA sola fila (expresable como update condicionado sobre su PK), sin namespace de lock nuevo ni migración.
- **Fix aplicado**: Se extrajo `aplicarMovimientoTx(tx, usuarioId, clienteId, input)` que opera sobre una transacción existente (`type MonederoTx = Parameters<Parameters<TenantPrismaClient["$transaction"]>[0]>[0]`); `moverMonedero` queda como wrapper delgado (`client.$transaction((tx) => aplicarMovimientoTx(...))`), firma pública sin cambios. En `canjearGiftCardAMonedero` los reads previos quedan como guardas UX; la compuerta autoritativa es un `client.$transaction` que: (a) CAS `tx.tarjetaRegalo.updateMany({ where:{ id, status:"activa" }, data:{ saldoActual:"0", status:"agotada", canjeadaAt:new Date() } })`; (b) si `consumed.count === 0` lanza 409 ANTES de acreditar; (c) acredita vía `aplicarMovimientoTx` en la misma tx (si falla, rollback del consumo). Garantía: N canjes simultáneos del mismo código → exactamente 1 abono; el resto recibe 409. Cierra el TOCTOU sin advisory lock ni migración.
- **Endurecimiento recomendado (fuera de esta vuln, no aplicado)**: sustituir el read-compute-write de `saldoMonedero` en `aplicarMovimientoTx` por `increment`/`decrement` atómicos + `updateMany` condicionado para el invariante no-negativo, eliminando el lost-update entre movimientos concurrentes del mismo cliente (solo mitiga sub-acreditación, no el over-credit reportado). Backstop adicional posible: unique en `MonederoMovimiento(refTipo, refId)` (requiere migración).

### R3-03 · MEDIUM · Operaciones de cobranza (anular/reprogramar facturas) sin gate de rol (broken-authorization)

- **Archivo**: `apps/api/src/modules/admin/billing-ops-routes.ts`
- **Explotación**: `adminBillingOpsRoutes` aplicaba solo `authenticateAdmin` (valida `kind === "admin"` pero NO `role`). `POST /invoices/:id/void` (write-off que deja de cobrarse, `status → void`, `nextRetryAt → null`) y `POST /invoices/:id/reintentar` (reprograma dunning) quedaban accesibles a CUALQUIER admin, incluido `support` que no debería tocar cobranza. Un admin `support` podía anular facturas `open`/`draft`/`uncollectible` de cualquier tenant, cancelando ingresos de la plataforma sin autorización de superadmin. Los módulos hermanos del mismo directorio (`tenants-routes`, `team-routes`, `catalogo-routes`) sí gatean por `role === "superadmin"`, probando que aquí faltaba el check.
- **Decisión del panel**: Aprobado gate de rol a nivel de operación mutante (consistente con los módulos hermanos). Se restringe la operación de cobranza a los roles que legítimamente la ejecutan (`superadmin` y el rol dedicado `billing`); `support` queda excluido.
- **Fix aplicado**: Constante `ROLES_COBRANZA = new Set(["superadmin", "billing"])` y helper `requireCobranza(req, reply)` que retorna 403 "Solo superadmin o rol de cobranza puede operar facturas" salvo que `req.user.kind === "admin" && ROLES_COBRANZA.has(req.user.role)`. El narrowing `kind === "admin"` tipa `role` sin `any` (TS strict). Se invoca como primera línea en `POST /invoices/:id/void` y `POST /invoices/:id/reintentar`; los GET (`/overview`, `/invoices`, `/subscriptions`) siguen accesibles a cualquier admin. La operación queda además en `writeAudit` con actor/motivo.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores.

---

## Ronda 4 — 2026-07-28

Rama: `seguridad/auditoria`. 5 vulnerabilidades aprobadas por el panel y aplicadas.

### R4-01 · HIGH · Sync push crea/edita clientes sin `CLIENTES_CREAR`/`CLIENTES_ACTUALIZAR` (broken-authorization)

- **Archivo**: `apps/api/src/modules/tenant/sync/service.ts`
- **Explotación**: El push de sync offline gateaba permisos por tipo de operación solo para `venta`. Una op `cliente` se despachaba a `aplicarCliente`, que hacía `prisma.cliente.create` y `prisma.cliente.update` SIN ningún `hasPermission`. La ruta (`sync/routes.ts`) solo exige `SYNC_USAR` más un `VENTAS_CREAR` condicional. `aplicarVenta` sí re-chequeaba `VENTAS_CREAR` ("sync.usar no basta"), pero el gate equivalente para clientes se omitió. El vector es alcanzable con un rol preset por defecto: `cajero` tiene `SYNC_USAR` y `CLIENTES_LEER` pero NO `CLIENTES_CREAR`/`CLIENTES_ACTUALIZAR`, mientras `vendedor` sí — probando que los permisos son una frontera de privilegio real. Un cajero que recibe 403 en `PATCH /t/clientes/:id` podía en su lugar `POST /t/sync/push` con una op `cliente` update/create para sobrescribir nombre, apellidos, emailPrincipal, telefonoPrincipal, rfc, notas, aceptaMarketing de cualquier cliente, o inyectar clientes arbitrarios. Gap secundario: `aplicarCliente` tampoco respetaba el guard read-only de `isDefault` que sí aplica el `PATCH` directo.
- **Decisión del panel**: Ganadora propuesta 1 (consenso unánime 3/3). Mover el gate RBAC a la capa de servicio dentro de `aplicarCliente` replicando el patrón defensa-en-profundidad de `aplicarVenta` (gate por operación en el servicio, NO en la ruta), sin tocar `routes.ts` ni el esquema Zod, y cerrar además el gap de `isDefault`. El gate por operación en el servicio preserva la semántica de éxito parcial del sync: una op de cliente sin permiso falla como `failed` individual sin tumbar el batch mixto con un 403 global, y al no persistirse los `failed` permite reintento legítimo con la misma idempotencyKey si luego se otorga el permiso.
- **Fix aplicado**: Firma de `aplicarCliente` cambiada a `(prisma, principal, op)`. Gate RBAC al inicio, antes de cualquier `create`/`update`: `permisoRequerido = op.operation === "create" ? PERMISSIONS.CLIENTES_CREAR : PERMISSIONS.CLIENTES_ACTUALIZAR`; si `!hasPermission(principal, permisoRequerido)` retorna `status:"failed"` con `entityIdRemoto: op.entityIdRemoto ?? null` (evita filtrar `undefined` a un campo requerido bajo `exactOptionalPropertyTypes`). Guard `if (current.isDefault)` en la rama update tras el `findUnique`, retornando `failed` "El cliente Público en general es de solo lectura" (espeja `clientes/routes.ts:144`). Call site en `procesarPush` actualizado a `aplicarCliente(prisma, principal, op)`. Sin imports nuevos (`PERMISSIONS`, `hasPermission`, `PermissionPrincipal` ya importados). Aislamiento por tenant vía `req.tenantPrisma` intacto.

### R4-02 · HIGH · Reseñas de marketplace sin autenticación: `pacienteEmail` del body confiable, badge "verificada" forjable (broken-authentication / input-validation)

- **Archivos**: `apps/api/src/modules/marketplace/routes.ts`, `apps/api/src/modules/marketplace/schemas.ts`, `apps/api/src/modules/marketplace/service.ts`
- **Explotación**: `POST /marketplace/profesionales/:id/resenas` (en `marketplacePublicRoutes`, "sin auth") no tenía autenticación. La identidad del reseñador se tomaba enteramente del campo body `pacienteEmail`: el código solo comprobaba que existiera un `pacienteMaster` con `otpVerificadoAt`, nunca que el llamador controlara ese email. Peor, `verificada` (el badge "visita verificada") se ponía como `Boolean(input.bookingId)` SIN validar que el `bookingId` fuera una cita real del paciente, con ese profesional, completada — cualquiera podía estampar una reseña como visita verificada pasando un `bookingId` arbitrario. Un atacante registraba cualquier email en `POST /marketplace/pacientes/registro`, confirmaba el OTP en su propia bandeja, y publicaba reseñas 5★ o 1★ sobre cualquier profesional (astroturfing / review-bombing) con badge de visita verificada forjado; reseñas rating-only auto-publicaban (sin moderación) y movían el score público agregado.
- **Decisión del panel**: Ganadora propuesta 3 (consenso unánime 3/3). (1) Ligar la identidad del reseñador al JWT de paciente vía `preHandler app.authenticatePatient` en vez de confiar en el body; (2) eliminar `pacienteEmail` de `crearResenaPublicaSchema`; (3) validar `bookingId` contra una cita real (`pacienteMasterId` propio, `professionalId` coincidente, `status === "completada"`) antes de otorgar `verificada`, con 403 si no cumple. Reutiliza infraestructura de auth existente (`authenticatePatient` ya protege `/pacientes/:id/reservas`), riesgo de regresión bajo. El rate-limit por-ruta es hardening opcional (ya existe límite global 100/min).
- **Fix aplicado**:
  - **schemas.ts**: eliminado el campo `pacienteEmail` de `crearResenaPublicaSchema`; la identidad deja de viajar en el body.
  - **routes.ts**: ruta registrada con `{ preHandler: app.authenticatePatient }` (JWT `kind:"patient"` emitido tras OTP); el handler llama `crearResena(app.masterPrisma, { professionalId: id, pacienteMasterId: req.user.sub, ...body })`.
  - **service.ts**: `CrearResenaInput.pacienteEmail` reemplazado por `pacienteMasterId`. Lookup del paciente por `id` (404 si no existe); se conserva el check `otpVerificadoAt` como defensa en profundidad. `verificada` calculada validando el booking real: `findUnique` en `publicBooking` y `verificada = booking !== null && booking.pacienteMasterId === paciente.id && booking.professionalId === input.professionalId && booking.status === "completada"`, con `throw MarketplaceError(403)` si el `bookingId` no es válido; `publicReview.create` usa la variable validada, no `Boolean(input.bookingId)`. Adicionalmente `confirmarPacienteMaster` deja de ser idempotente (emite sesión PHR real): cada confirmación exige código pendiente/vigente/coincidente y lo consume. Usa `masterPrisma` (PacienteMaster, PublicBooking y PublicReview viven en master DB cross-tenant).

### R4-03 · HIGH · Webhook de pago del checkout no idempotente: entregas duplicadas crean ventas dobles y descuentan inventario dos veces (webhook-idempotency-race)

- **Archivo**: `apps/api/src/modules/tenant/checkout/service.ts`
- **Explotación**: `procesarWebhookPago` protegía contra reprocesamiento con un check-then-act plano: leía `pedido.statusPago` y, si no era `pago_confirmado`, ejecutaba `crearVenta` (descuenta stock) y luego actualizaba el pedido. Sin transacción que envolviera read+act, sin row-lock, sin tabla de eventos procesados, y `paymentIntentId` es nullable y no `@unique`. Los procesadores (Stripe/Conekta) entregan at-least-once y reintentan en timeout, así que dos entregas concurrentes del mismo `payment_intent.succeeded`/`order.paid` leían ambas `statusPago='pendiente'`, ambas pasaban el guard, ambas llamaban `crearVenta` → dos ventas ecommerce con folios distintos y DOS descuentos de stock por un solo pago (corrupción de inventario, posible oversell, CFDI duplicado). El check de monto no ayuda: ambas entregas llevan el mismo monto firmado válido. El módulo hermano `devoluciones` ya cerraba este TOCTOU con `pg_advisory_xact_lock`; checkout lo omitía.
- **Decisión del panel**: Ganadora propuesta 2. Cerrar el TOCTOU con un claim atómico compare-and-swap sobre el estado del pedido (`updateMany` con guarda de estado) que Postgres serializa a nivel de fila: solo una entrega transiciona el pedido a `pago_confirmado` y ejecuta `crearVenta`; las duplicadas obtienen `count=0` y salen idempotentes. El CAS es preferible al advisory lock aquí porque el invariante a proteger es el `statusPago` de UNA fila, expresable como update condicionado sobre su PK, sin namespace de lock nuevo ni migración.
- **Fix aplicado**: Tras el check de monto, claim atómico `client.pedidoEcommerce.updateMany({ where: { id, statusPago: { not: "pago_confirmado" } }, data: { statusPago: "pago_confirmado" } })`. Si `claim.count === 0`, relee el pedido (`findUniqueOrThrow`) y retorna idempotente con el estado/`ventaIdGenerada` ya confirmados, sin re-ejecutar `crearVenta`. `crearVenta` queda envuelto en `try/catch`: si falla, libera el claim con guarda `ventaIdGenerada: null` (`updateMany` de vuelta a `estadoPrevio`) para permitir reintento del proveedor sin revertir un pedido ya completado, y re-lanza. El `update` final ya no re-escribe `statusPago` (fijado por el claim). Aislamiento por tenant vía `client` (tenantPrisma) intacto.

### R4-04 · HIGH · Doble canje de puntos de lealtad: check de saldo fuera de la transacción y update de valor absoluto (race-condition / TOCTOU)

- **Archivo**: `apps/api/src/modules/tenant/lealtad/service.ts`
- **Explotación**: `canjearPuntos` leía `inscrito.puntosActuales` con un `findUnique` FUERA de toda transacción, validaba `puntosActuales >= puntos`, computaba `nuevoSaldo = puntosActuales - puntos`, y en un `$transaction([...])` batch escribía `puntosActuales = nuevoSaldo` como valor ABSOLUTO más un movimiento `canje` por `valorMxn` pesos. Como el read/check no se serializa y el write sobrescribe en vez de decrementar, dos canjes concurrentes leían ambos el saldo completo, ambos pasaban el check, y ambos persistían un canje por MXN real, dejando `puntosActuales` en el mismo `nuevoSaldo` (el cliente gasta los mismos puntos dos veces). Con 1000 puntos ($200), dos `POST /lealtad/canjear` concurrentes → $400 de valor por 1000 puntos. Gateado por `LEALTAD_GESTIONAR` (requiere insider/cajero con doble-submit), explotable hoy con pérdida MXN directa.
- **Decisión del panel**: Ganadora propuesta 1 (consenso unánime 3/3). Reemplazar el TOCTOU por un decremento condicional atómico dentro de una transacción interactiva: `tx.clienteLoyalty.updateMany({ where: { id, puntosActuales: { gte: puntos } }, data: { puntosActuales: { decrement }, lifetimeCanjeado: { increment } } })` y rechazo 409 cuando `count===0`. Bajo Read Committed, el `UPDATE ... WHERE puntosActuales >= N` toma el row lock y re-evalúa la condición contra el valor ya decrementado, por lo que el segundo canje matchea 0 filas y nunca crea el segundo movimiento.
- **Fix aplicado**: Se elimina el pre-check en JS y el cómputo `nuevoSaldo` basado en la lectura stale; el `findUnique` inicial se conserva solo para el 404 de cliente no inscrito y resolver `inscrito.id`. El `$transaction([...])` batch se reemplaza por `$transaction(async (tx) => {...})` interactivo con `updateMany` condicional (decrement `puntosActuales`, increment `lifetimeCanjeado`); `count===0` lanza `LealtadError(409, "Saldo de puntos insuficiente")` con rollback automático. `saldoResultante` se toma de un `findUniqueOrThrow` post-decremento DENTRO de la tx (valor serializado real). `lifetimeCanjeado` pasa de set absoluto a increment relativo, consistente con `acumularPuntos`. Sin `any`, `client = req.tenantPrisma`, sin cambio de firma.
- **Endurecimiento opcional (no aplicado)**: idempotency key en `POST /lealtad/canjear` para deduplicar el doble-submit del cajero a nivel de request; el decremento atómico por sí solo ya impide el doble-canje de valor MXN.

### R4-05 · HIGH · Sobregiro de monedero (store-credit): débito lee saldo y escribe sin row-lock (race-condition)

- **Archivos**: `apps/api/src/modules/tenant/monedero/service.ts`, `apps/api/src/modules/tenant/ventas/service.ts`
- **Explotación**: `aplicarMovimientoTx` (usado por `moverMonedero` y, en el path de venta, `aplicarCargoMonedero` en `ventas/service.ts`) leía `cliente.saldoMonedero`, computaba `nuevo = saldoActual + delta`, rechazaba si `nuevo < 0`, y luego escribía el valor ABSOLUTO. El read no era locking (sin `SELECT FOR UPDATE` ni advisory lock) y el write era asignación absoluta, así que bajo READ COMMITTED dos operaciones `cargo` concurrentes leían el mismo saldo, ambas pasaban el check no-negativo, y la segunda escritura sobrescribía la primera (lost update). Con `saldoMonedero=100`, dos ventas de 100 con `metodo='monedero'` (o dos `POST /monedero/clientes/:id/movimiento` cargo) sobre el mismo cliente → se consumen 200 de un saldo de 100. A diferencia del canje de gift-card (endurecido con `updateMany` CAS), el path de monedero no tenía protección.
- **Decisión del panel**: Ganadora propuesta 3. Compare-and-swap atómico: decremento condicional `updateMany({ where: { id, saldoMonedero: { gte: monto } }, data: { saldoMonedero: { decrement: monto } } })` que toma lock de fila en Postgres, así dos cargos concurrentes se serializan y el segundo re-evalúa el where contra el saldo ya descontado (`count=0` = insuficiente), cerrando el lost-update. El abono usa `increment` relativo por la misma razón. Aplicado en AMBOS entrypoints (movimiento directo y path de venta).
- **Fix aplicado**:
  - **monedero/service.ts** (`aplicarMovimientoTx`): rama `cargo` usa `tx.cliente.updateMany({ where: { id, saldoMonedero: { gte: montoStr } }, data: { saldoMonedero: { decrement: montoStr } } })`; si `count===0` distingue 404 (cliente inexistente) de 409 ("Saldo insuficiente en el monedero"). Rama `abono` usa `increment` con `updateMany` (404 si `count===0`). El `saldoResultante` del movimiento se relee post-CAS dentro de la tx.
  - **ventas/service.ts** (`aplicarCargoMonedero`, path de venta con pago monedero): mismo CAS `updateMany` con `saldoMonedero: { gte } / { decrement }`; si `count===0` relee el saldo actual para el mensaje `VentaError(409, "Saldo insuficiente en el monedero", { saldo, requerido })`; el saldo resultante del movimiento se relee post-decremento. Sin `any`, `client = req.tenantPrisma`, sin cambio de firma.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores.

---

## Ronda 5 — 2026-07-28

Rama: `seguridad/auditoria`. 2 vulnerabilidades aprobadas por el panel y aplicadas.

### R5-01 · HIGH · Fiado (store-credit): bypass del límite y lost-update por ventas concurrentes (race-condition)

- **Archivo**: `apps/api/src/modules/tenant/clientes/fiado-service.ts`
- **Explotación**: `aplicarCargoFiado` (invocado dentro del `$transaction` de `crearVenta` por cada pago `metodo=credito_fiado`) leía el saldo con un `findUnique` plano, validaba `totalNuevo <= limiteFiado`, y luego escribía el nuevo saldo como valor ABSOLUTO (`tx.fiado.update({ data: { montoTotal: totalNuevo.toString() } })`). Sin row-lock, sin advisory lock, sin `updateMany` condicional compare-and-swap, y con la isolation por defecto de Postgres (READ COMMITTED). Es el mismo patrón lost-update que los paths de monedero (`aplicarCargoMonedero`) y devoluciones ya cerraron con CAS/advisory locks, pero el path de fiado quedó sin guardar. Dos `POST /ventas` cargando el mismo `clienteId` a `credito_fiado` en paralelo leían ambas `montoTotal=X`, ambas pasaban el check de límite, y ambas sobrescribían `montoTotal` con su propio `X+monto`: un cargo se pierde silenciosamente Y el límite se sobrepasa. Con `limiteFiado=1000`, `montoTotal=0`, dos ventas concurrentes de 600 a fiado → el cliente sale con $1200 de mercancía, el límite de $1000 se excede, y el ledger registra solo $600 de deuda (la tienda pierde $600 de deuda rastreada). Alcanzable por cualquier usuario con `VENTAS_CREAR` (cajero), incluso por un doble-submit accidental.
- **Decisión del panel**: Ganadora propuesta 1 (consenso unánime 3/3). Reemplazar el read-then-absolute-write por un compare-and-swap atómico con `tx.fiado.updateMany`, replicando el patrón ya validado en `aplicarCargoMonedero`: calcular `maxActualPermitido = limite - monto`, cortocircuitar a `count:0` cuando un solo cargo ya excede el límite, ejecutar un `updateMany` condicional (`where montoTotal <= maxActualPermitido`) con `increment` atómico que toma lock de fila bajo READ COMMITTED, re-leer y lanzar el mismo `FiadoError(409)` preservando el contrato de UI cuando `count===0`, y usar `increment` (no valor absoluto) en la rama sin límite.
- **Fix aplicado**: En `aplicarCargoFiado`, rama `limite.gt(ZERO)`: `maxActualPermitido = limite.minus(monto)`; si `maxActualPermitido.lt(ZERO)` → `{ count: 0 }` (rechazo sin tocar BD cuando un solo cargo ya excede el límite), si no `tx.fiado.updateMany({ where: { id: fiado.id, montoTotal: { lte: maxActualPermitido.toString() } }, data: { montoTotal: { increment: monto.toString() }, fechaUltimoMovimiento, estado: "activo" } })`. Si `charged.count === 0`, re-lee `montoTotal` y lanza `FiadoError(409, "Excede el límite de fiado autorizado", { limite, totalActual, intentado, disponible })` — contrato idéntico para no romper la UI. Rama sin límite (`limiteFiado==0`) usa `increment` (nunca valor absoluto), cerrando también el lost-update aunque no haya tope. Equivalencia semántica preservada: `totalActual + monto <= limite ⇔ montoTotal <= limite - monto`. `fiadoMovimiento` intacto; sin `any`; Prisma Decimal acepta string en `lte`/`increment`; usa el `tx` del `$transaction` de `crearVenta` (tenantPrisma), sin tocar `masterPrisma` ni `ensureFiado`/`aplicarAbonoFiado`/`regularizarFiadoToCxc`.

### R5-02 · HIGH · Sobregiro de línea de crédito B2B por TOCTOU (chequeo de crédito fuera de la transacción) (race-condition)

- **Archivos**: `apps/api/src/modules/tenant/ventas/service.ts`, `apps/api/src/modules/tenant/cxc/service.ts`
- **Explotación**: En `crearVenta`, para un pago `metodo=credito_b2b`, `validarCreditoB2bSuficiente` se invocaba ANTES de abrir `client.$transaction`. Ese validador sumaba los saldos de todas las CxC abiertas del cliente y comprobaba que el monto solicitado cupiera bajo `lineaAutorizada`. La CxC que consume el crédito solo se creaba después, dentro de la transacción, vía `crearCxcDesdeVentaB2b`. Como el check leía el estado committed enteramente fuera y antes de la transacción de escritura, sin lock ni serializable isolation sobre la línea de crédito (el `$transaction` corre a READ COMMITTED por defecto), dos ventas B2B a crédito concurrentes del mismo `clienteB2bId` observaban el mismo conjunto de CxC abiertas, ambas pasaban el check de disponible, y ambas creaban CxC, empujando el crédito total por encima de la línea autorizada. Con `lineaAutorizada=100000` y 0 CxC abiertas, dos `POST /ventas` concurrentes de 90000 a `credito_b2b` → ambas ven `disponible=100000`, ambas pasan, ambas crean una CxC de 90000: el cliente debe 180000 contra una línea de 100000 (sobregiro del 80%). Alcanzable por cualquier usuario con `VENTAS_CREAR`, incluida la PWA de vendedor mayoreo en campo.
- **Decisión del panel**: Ganadora propuesta 1. Cerrar el TOCTOU serializando las ventas a crédito concurrentes del mismo cliente con un `pg_advisory_xact_lock` por cliente B2B tomado DENTRO de la transacción, y mover el chequeo de disponibilidad al interior del `$transaction` para que se evalúe contra el estado ya comprometido por la transacción que entra primero. El advisory lock es preferible al CAS aquí porque el invariante no reside en una sola fila (la disponibilidad se computa dinámicamente sumando N filas `CuentaCobrar` abiertas) y el esquema solo almacena `lineaAutorizada` sin un contador de crédito usado que decrementar; no requiere migración.
- **Fix aplicado**:
  - **cxc/service.ts**: nueva constante `CREDITO_B2B_LOCK_NAMESPACE = 9274183n` y helper `lockCreditoB2b(tx, clienteB2bId)` que ejecuta `SELECT pg_advisory_xact_lock(hashtextextended(${clienteB2bId}, ${CREDITO_B2B_LOCK_NAMESPACE}))`. `validarCreditoB2bSuficiente` cambia su firma de `(client: TenantClient, ...)` a `(tx: Tx, ...)` y toma el advisory lock como primera operación, antes de `lineaCreditoDisponible(tx, ...)`; el lock por-transacción se libera automáticamente al COMMIT/ROLLBACK. `lineaCreditoDisponible` amplía su parámetro a `TenantClient | Tx` para poder ejecutar dentro de la transacción.
  - **ventas/service.ts** (`crearVenta`): se elimina el bloque `validarCreditoB2bSuficiente` previo a `client.$transaction` (con su `try/catch` de `CxcError`) y se mueve DENTRO del `$transaction`, invocándolo con `tx` en lugar de `client`. El mapeo `CxcError → VentaError` ya lo cubre el `catch` que envuelve la transacción. Aislamiento por tenant vía `client`/`tx` (tenantPrisma) intacto; sin `any`; sin migración.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores.

---

## Ronda 6 — 2026-07-28

Rama: `seguridad/auditoria`. 3 vulnerabilidades aprobadas por el panel y aplicadas.

### R6-01 · HIGH · Acuñación de saldo de monedero gateada por permiso de bajo privilegio (broken-access-control)

- **Archivo**: `apps/api/src/modules/tenant/monedero/routes.ts`
- **Explotación**: `POST /t/monedero/clientes/:clienteId/movimiento` acepta `{tipo:"abono"|"cargo", monto}` y llama a `moverMonedero → aplicarMovimientoTx`, que suma saldo de monedero gastable (dinero real, canjeable en checkout) a la cartera de cualquier cliente. La ruta estaba protegida solo por `PERMISSIONS.CLIENTES_ACTUALIZAR` (el permiso genérico "editar perfil de cliente" que se otorga rutinariamente a cajeros/recepción). El schema Zod solo valida forma (monto hasta 1,000,000) y la rama `abono` incrementa `saldoMonedero` sin verificación financiera. Inconsistente con el propio modelo del codebase: fiado/abonar exige `CLIENTES_FIADO_GESTIONAR` (clientes/routes.ts:290) y gift-cards exige `VENTAS_CREAR`. Roles preset infra-privilegiados (`vendedor`, recepción vet) tienen `clientes.actualizar` pero NO `clientes.fiado_gestionar`, así que un rol de captura podía acuñar dinero. Mismo gate débil en `POST /gift-cards/canjear` (mover valor a la cartera de un cliente elegido). Explotación: un empleado con solo `clientes.actualizar` acredita 5000 de saldo gastable a una cuenta cómplice sin recibir efectivo, y luego se paga mercancía en checkout o se retira/reembolsa como efectivo → robo financiero directo.
- **Decisión del panel**: Ganadora propuesta 1 (consenso unánime 3/3). Reemplazar el gate genérico `PERMISSIONS.CLIENTES_ACTUALIZAR` por el permiso financiero ya existente `PERMISSIONS.CLIENTES_FIADO_GESTIONAR` en las dos rutas que mueven dinero real hacia el monedero (`POST /gift-cards/canjear`; `POST /clientes/:clienteId/movimiento` abono/cargo). Cierra la escalada porque los roles preset infra-privilegiados carecen de `clientes.fiado_gestionar`; consistente con fiado/abonar; sin permiso nuevo, sin migraciones, sin tocar catalog/preset-roles/schemas/service. La rama `cargo` (retirar saldo real) también queda correctamente gateada como operación financiera. Dueño/gerente y dueño con wildcard `*` conservan acceso.
- **Fix aplicado**: En `monedero/routes.ts`, `POST /gift-cards/canjear` (línea 70) y `POST /clientes/:clienteId/movimiento` (línea 98) usan `req.requirePerm(PERMISSIONS.CLIENTES_FIADO_GESTIONAR)` en lugar de `CLIENTES_ACTUALIZAR`. `PERMISSIONS.CLIENTES_FIADO_GESTIONAR` ya se importa vía `@gaespos/permissions` (línea 1); sin imports nuevos, sin cambios de tipos, sin impacto en TS strict. Otras rutas intactas: `GET /clientes/:clienteId` sigue con `CLIENTES_LEER`; `POST /gift-cards` y `/gift-cards/:id/cancelar` con `VENTAS_CREAR`; `GET /gift-cards*` con `VENTAS_LEER`. Seguimiento recomendado (fuera de scope backend): ocultar botones Abonar/Cobrar/Canjear en `MonederoPage.tsx` para usuarios sin `clientes.fiado_gestionar` vía `puede()`.

### R6-02 · MEDIUM · Precio negativo persistible habilita robo de mercancía gratis en carritos mixtos (input-validation)

- **Archivos**: `apps/api/src/modules/tenant/listas-precios/schemas.ts`, `apps/api/src/modules/tenant/inventario/schemas.ts`, `packages/pricing/src/calculate.ts`
- **Explotación**: El `decimalString` compartido admitía signo negativo (`z.number().finite()` + `/^-?\d+(\.\d+)?$/`) en campos de dinero. El vector de ingreso original (`precioBase` en productos/variantes) ya estaba cerrado en rondas previas con `min(0)`, pero la misma clase de vulnerabilidad seguía abierta por `listas-precios/schemas.ts`: `accion.valor` de una regla de precio (`reglaCreateSchema`/`reglaUpdateSchema`, tipo `precio_override`) aceptaba negativos. El motor de precios NO tenía piso por línea: `calcularLinea` computaba `subtotal = precioFinal.mul(cantidad)` sin clamp (calculate.ts:199), y solo el total final del ticket se clampeaba con `Decimal.max(total, ZERO)`. Por tanto una línea con precio negativo resta del subtotal de las otras líneas positivas de la misma venta: laptop 10000 + regla `{tipo:precio_override, valor:-10000}` sobre un producto acompañante → subtotal 0, ticket clampa a 0, venta registrada como cobrada $0 con stock descontado en ambas líneas y arqueo de caja cuadrando (robo silencioso). Alcanzable por un usuario con permiso de edición de reglas de precio. `inventario/schemas.ts` compartía el mismo `decimalString` negativo en `stockMinimo`/`stockMaximo`/`costoUnitario` (higiene de misma clase).
- **Decisión del panel**: Ganadora propuesta 2, integrada con propuesta 3 (defensa en profundidad). La propuesta 1 quedó descartada (su premisa —`precioBase` en productos/variantes— ya estaba corregida, era un no-op). Fix consensuado en 2 capas: **Capa 1 (input)** cierra el vector explotable hoy en `listas-precios` (único uso de ese `decimalString`, en `accion.valor` líneas 99 y 118) y aplica la misma higiene a `inventario`; **Capa 1b** cubre `inventario`; **Capa 2 (motor)** añade un piso por línea que ataja valores negativos persistidos por cualquier vía (DB directa, regresión de schema, porcentaje>100). Complementarias, no rivales. TS strict, sin `any`, sin migraciones.
- **Fix aplicado**:
  - **listas-precios/schemas.ts** (líneas 3-5): `decimalString` pasa de `z.union([z.number().finite(), z.string().regex(/^-?\d+(\.\d+)?$/)])` a `z.union([z.number().finite().min(0), z.string().regex(/^\d+(\.\d+)?$/)])`. Fuerza `accion.valor >= 0` en `reglaCreateSchema` y `reglaUpdateSchema` para porcentaje/monto_fijo/precio_override. `positiveDecimalString` intacto.
  - **inventario/schemas.ts** (líneas 3-5): idéntico cambio a `min(0)` + `/^\d+(\.\d+)?$/` (usado en `stockMinimo`, `stockMaximo`, `costoUnitario`; el signo del ajuste lo lleva el enum `tipo`, no el monto).
  - **packages/pricing/src/calculate.ts** (línea 199): `const subtotal = precioFinal.mul(cantidad);` → `const subtotal = Decimal.max(precioFinal.mul(cantidad), ZERO);` (`ZERO` ya existía). Un subtotal solo puede quedar negativo con precio/override negativo, nunca en flujo legítimo (devoluciones van por su propio módulo); neutraliza el mecanismo de resta entre líneas aunque un negativo se persista por otra vía.
  - **Fuera de scope (backlog)**: acotar `accion.valor` de tipo `porcentaje` a 0-100 y `CHECK (precio_base >= 0)` vía migración multi-tenant.

### R6-03 · LOW · Config de proveedor de recargas devuelve apiKey/webhookSecret en texto plano en la respuesta (crypto-secrets)

- **Archivo**: `apps/api/src/modules/tenant/recargas/routes.ts`
- **Explotación**: `PUT /proveedores/:codigo/config` hacía upsert y devolvía la fila completa con `reply.code(200).send(cfg)`, sin response schema Fastify ni hook de redacción. Las columnas `apiKeyEncrypted`/`webhookSecretEncrypted` NO están cifradas (el sufijo `Encrypted` es un misnomer: no existe helper de cifrado; `routes.ts:198-205` pasa `cfg.apiKeyEncrypted` directo como `apiKey` viva al provider factory). Por tanto la respuesta reflejaba las credenciales reales del proveedor de recargas en claro. Cualquier cliente, proxy, extensión o log sink que capture la respuesta del save de config —o un usuario con `RECARGAS_CONFIGURAR` que solo modifique un campo— obtenía las credenciales para consumir el saldo prefondeado del tenant contra el proveedor out-of-band. Severidad baja: requiere un usuario ya autorizado o interceptor de sus respuestas; es exposición de secreto no cifrado en respuesta de endpoint de escritura.
- **Decisión del panel**: Ganadora propuesta 3 (consenso unánime en el enfoque). Redactar los secretos en la respuesta del PUT mediante un helper `redactProveedorConfig(cfg)` que hace destructuring de `apiKeyEncrypted`/`webhookSecretEncrypted` y devuelve el resto más dos flags booleanos (`apiKeyConfigurado`/`webhookSecretConfigurado`), sin tocar persistencia, permisos ni el provider factory. Mismo patrón DTO-seguro que ya usa `consultarSaldos`; es el único punto de fuga (no existe GET de config). Redacción en el objeto antes del serializador por defecto; no requiere response schema.
- **Fix aplicado**: En `recargas/routes.ts`, helper `redactProveedorConfig` (líneas 56-67) que elimina los dos campos sensibles del payload y los reemplaza por `apiKeyConfigurado`/`webhookSecretConfigurado` (booleanos, `string | null` siempre presentes → sin conditional spread, compatible con `exactOptionalPropertyTypes`). El handler `PUT /proveedores/:codigo/config` (línea 134) devuelve `reply.code(200).send(redactProveedorConfig(cfg))` en lugar de `send(cfg)`. `buildUpsertConfigData` y el upsert intactos; el provider factory (`POST /`) sigue leyendo `cfg.apiKeyEncrypted` directo de DB, las recargas no se rompen. Único helper de redacción, sin duplicados. **Fuera de alcance (backlog, no bloquea)**: las columnas se guardan en texto plano en reposo; el cifrado at-rest (`createCipheriv`/AES) es un cambio mayor separado a rastrear como issue propio.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores. `pnpm --filter @gaespos/pricing exec tsc --noEmit` → 0 errores.

---

## Ronda 7 — 2026-07-28

Rama: `seguridad/auditoria`. 1 vulnerabilidad aprobada por el panel y aplicada.

### R7-01 · LOW · Endpoints de wishlist sin gate de permiso y sin object-scoping (broken-authorization / IDOR horizontal)

- **Archivo**: `apps/api/src/modules/tenant/wishlists/routes.ts`
- **Explotación**: El módulo se registra bajo `/t/wishlists` (autenticado por tenant/staff) pero ninguno de los 4 handlers llamaba `req.requirePerm(...)` — era uno de los pocos módulos tenant sin gate. `GET /` lista wishlists de cualquier cliente filtrando solo por el `clienteId` que envía el atacante; `POST /` crea wishlist para cualquier `clienteId`; `POST /:id/items` agrega ítems a cualquier wishlist; y `DELETE /:id/items/:itemId` borraba el `wishlistItem` puramente por `itemId` (`req.tenantPrisma.wishlistItem.delete({ where: { id: params.itemId } })`) sin verificar que el ítem perteneciera a la wishlist `:id`. Residual explotable HOY (least-privilege interno): un empleado de baja jerarquía autenticado (p.ej. cajero) sin permiso de ecommerce podía enumerar/borrar wishlists de clientes dentro de su propio tenant. El vector comprador-a-comprador vía BFF del storefront quedó REFUTADO en verify: el storefront usa `/cliente-portal/wishlist`, ya scopeado por el `clienteId` autenticado y con verificación `item.wishlistId===wishlistId` antes de borrar; `tenant-context.ts` exige `kind==='tenant'` (rechaza JWT de comprador con 401) y `getTenantClient` aísla por schema. Datos de baja sensibilidad, requiere insider autenticado, confinado a un tenant → severidad low.
- **Decisión del panel**: Ganadora propuesta 2 (consenso unánime 3/3, `riesgoRegresion=bajo`). Gatear los 4 handlers con la familia `ECOMMERCE_PEDIDOS_*` A PROPÓSITO y NO con `CLIENTES_LEER`: un cajero típicamente YA tiene `CLIENTES_LEER`, por lo que gatear con ese permiso NO detendría el exploit; `ECOMMERCE_PEDIDOS_*` sí lo bloquea con 403. Además cerrar el IDOR de object-scoping en `DELETE` acotando el borrado a la wishlist `:id` con `deleteMany` y 404 si no pertenece (mismo criterio que `quitarDeWishlist` en cliente-portal). NO se toca `cliente-portal` (ya scopeado por `clienteId` autenticado) ni se agrega scoping por `clienteId` en la ruta tenant (el contexto es staff, no comprador). Sin permiso nuevo, sin migraciones.
- **Fix aplicado**: En `wishlists/routes.ts`:
  - Import `import { PERMISSIONS } from "@gaespos/permissions";` (una sola línea; códigos verificados en `packages/permissions/src/catalog.ts:121-122`).
  - Gate como primera línea de cada handler: `GET /` → `req.requirePerm(PERMISSIONS.ECOMMERCE_PEDIDOS_LEER)`; `POST /`, `POST /:id/items`, `DELETE /:id/items/:itemId` → `req.requirePerm(PERMISSIONS.ECOMMERCE_PEDIDOS_GESTIONAR)`.
  - `DELETE /:id/items/:itemId`: reemplazado el `delete({ where: { id: itemId } })` por `deleteMany({ where: { id: params.itemId, wishlistId: params.id } })`; si `count === 0` responde 404 (`Item no encontrado`), si no 204. Cierra el IDOR de object-scoping.
  - Se mantiene `req.tenantPrisma` (recurso por-tenant, aislado por schema); conditional spreads de `POST /` intactos (`exactOptionalPropertyTypes`); sin `any`, TS strict.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores (tras regenerar el cliente Prisma, que estaba desactualizado por trabajo no commiteado de otra ronda — no relacionado con este fix).

---

## Ronda 8 — 2026-07-28

Rama: `seguridad/auditoria`. 4 vulnerabilidades aprobadas por el panel y aplicadas.

### R8-01 · HIGH · Rate-limit bypass en todos los endpoints de auth vía X-Forwarded-For falsificable (`trustProxy: true`) (auth-rate-limit-bypass)

- **Archivos**: `apps/api/src/config.ts`, `apps/api/src/app.ts`
- **Explotación**: Fastify se construía con `trustProxy: true` (`app.ts:149`), lo que indica a `proxy-addr` que confíe en TODA la cadena `X-Forwarded-For` y devuelva la IP más a la izquierda —controlada por el cliente— como `req.ip`. El `@fastify/rate-limit` global (`plugins/security.ts`) se registra sin `keyGenerator` ni `allowList`, por lo que agrupa por `req.ip` (default). Como `req.ip` deriva de un header que el cliente controla, todos los límites por-ruta (`/auth/login` y `/auth/mfa/verify` 10/min, auth-tenant, partner-portal, b2b/cliente/billing, passkey, OTP) se evaden enviando un `X-Forwarded-For: <ip-aleatoria>` distinto por request: cada IP falsificada es un bucket nuevo, el cap de 10/min nunca se alcanza y se agota el keyspace del TOTP (6 dígitos) o del password. No existe defensa secundaria a nivel-cuenta para el TOTP admin/tenant (`verifyTotpStep` solo tiene anti-replay), así que el rate-limit por IP era la única barrera anti-fuerza-bruta. Además envenenaba el `ipAddress` de `writeAudit` (atribución forense destruida).
- **Decisión del panel**: Ganadora propuesta 3 (consenso unánime 3/3). Reemplazar `trustProxy: true` por un número EXACTO de saltos de proxy de confianza (`config.TRUST_PROXY_HOPS`, default 1 = Railway), sin tocar `@fastify/rate-limit`. Con un conteo fijo, `proxy-addr` recorta desde la derecha y devuelve la IP que puso el proxy real; ningún XFF inyectado por el atacante puede fabricar `req.ip`. Re-ancla el bucket del rate-limit global para TODAS las rutas login/mfa/OTP con un solo cambio en el origen y sanea el `ipAddress`. Degradado seguro (más proxies reales → límite más laxo, nunca más estricto). Fastify acepta `number` en `trustProxy` (TS strict sin `any`).
- **Fix aplicado**: `config.ts`: nuevo campo Zod `TRUST_PROXY_HOPS: z.coerce.number().int().nonnegative().default(1)` con comentario del porqué (Railway=1; 0 = ignorar XFF/usar socket; NUNCA `true`). `app.ts:149`: `trustProxy: true` → `trustProxy: config.TRUST_PROXY_HOPS` (`config` ya en scope de `buildApp`). `test/helpers.ts` (`makeTestConfig`): agregado `TRUST_PROXY_HOPS: 1` al literal de `Config` (campo ahora requerido). **Fuera de scope (hallazgo separado)**: falta lockout a nivel-cuenta en el TOTP admin/tenant y en los `/login` (defensa en profundidad frente a fuerza bruta desde una sola IP real).

### R8-02 · HIGH · Escalada de privilegios al editar/crear roles: sin chequeo de que el actor posea los permisos que otorga (broken-access-control / mass-assignment)

- **Archivo**: `apps/api/src/modules/tenant/roles/routes.ts`
- **Explotación**: `POST /roles` y `PATCH /roles/:id` dejaban fijar el array `permisos` de un rol a cualquier permiso, con guards únicos: `ROLES_CREAR`/`ROLES_ACTUALIZAR`, chequeo not-preset y `permisosFueraDeVertical()` (que solo rechaza `*`, permisos desconocidos y cross-vertical). NO había chequeo de que el actor ya poseyera los permisos que embebe en el rol. El login re-mergea `rol.permisos` en el JWT (`auth-tenant/service.ts` `mergeRolePermissions`), por lo que mutar un rol al que el usuario pertenece cambia sus permisos efectivos en el siguiente token. Un no-dueño con un rol delegado custom que incluya `ROLES_ACTUALIZAR` podía agregar todos los permisos in-vertical (excepto `*`) a un rol no-preset y escalar a admin casi total, evadiendo el guard de assign-time (`assertCanGrantRoles`) del módulo usuarios, incluyendo el bypass en dos pasos (crear rol con permisos propios → auto-asignarse → editar para agregar permisos elevados). Techo: wildcard `*` del dueño y cross-vertical.
- **Decisión del panel**: Ganadora propuesta 2 (consenso unánime 3/3). Espejar la invariante anti-escalada de `usuarios/routes.ts` (`assertCanGrantRoles`) pero en el momento de ESCRIBIR el rol. Helper `permisosNoPoseidos(req, permisos)`: `[]` si `req.principal.isOwner`; si no, filtra `*` o permisos ausentes del `Set` de `req.principal.permissions`; 403 en ambos endpoints tras el chequeo de vertical. Defensa en profundidad ADICIONAL a `permisosFueraDeVertical` (no lo reemplaza). Opera sobre el principal en memoria (sin `masterPrisma` extra), TS strict sin `any`; no toca `schemas.ts` (el techo por-actor es dinámico, no expresable en Zod).
- **Fix aplicado**: En `roles/routes.ts`, helper `permisosNoPoseidos(req, permisos)` (línea 108). Invocado en `POST /` (tras `permisosFueraDeVertical`, antes de `rol.create`, línea 125) y en `PATCH /:id` dentro del bloque `if (body.permisos)` (tras el chequeo de vertical, antes de `rol.update`, línea 173); si hay permisos no poseídos responde 403 (`No puedes otorgar permisos que tú no posees: ...`). Cierra la escalada por edición y el bypass de dos pasos. **Nota de exactitud**: ningún rol preset shipped otorga `ROLES_CREAR/ACTUALIZAR` (solo `dueno` tiene `*`, `gerente` solo `ROLES_LEER`), así que el exploit requiere que el dueño haya creado un rol delegado custom con permisos de gestión de roles — escenario realista e intencional que este guard protege.

### R8-03 · HIGH · El flujo de apartados evade el tope de descuento global manual → mercancía gratis/muy descontada (business-logic-abuse)

- **Archivos**: `apps/api/src/modules/tenant/apartados/service.ts`, `apps/api/src/modules/tenant/apartados/routes.ts`
- **Explotación**: `crearApartado()` reenviaba el `descuentoGlobalPct` del cliente directo a `calcularPreview()` SIN llamar a `validarTopeDescuento()`. Ese tope (`config_ventas.descuentoMaximoPct`, sobrepasable solo con `VENTAS_APLICAR_DESCUENTO_ALTO` o siendo dueño) existía SOLO en `crearVenta`. El motor de pricing aplica el porcentaje crudo sin clamp y clampea el total a cero. El schema de apartados validaba `descuentoGlobalPct` solo como decimal no-negativo sin cota superior. `liquidarApartado()` materializa los precios almacenados en una venta `cobrada` real, descuenta stock vía `aplicarAjuste` y solo exige `montoPagado >= total`; con total llevado a 0 por 100%, ese guard es `0 >= 0` y pasa. Un usuario con `APARTADOS_CREAR` + `APARTADOS_LIQUIDAR` pero SIN `VENTAS_APLICAR_DESCUENTO_ALTO` podía finalizar una venta a cualquier descuento hasta 100% (mercancía gratis), evadiendo el techo y el permiso que debía autorizar descuentos altos.
- **Decisión del panel**: Ganadora propuesta 1 (mayoría 2/3, funcionalmente idéntica a #2). Portar al flujo de apartados el control de `crearVenta` con superficie mínima (solo el módulo apartados), aplicándolo en `crearApartado` ANTES de `calcularPreview` (único punto donde el descuento manual entra al motor). Helper LOCAL autocontenido que lanza `ApartadoError(400)` (error correcto para el `handleErr` de apartados, evita un 500 por `VentaError` cross-módulo); `permiteDescuentoAlto` se resuelve en la ruta vía `hasPermission(req.principal, PERMISSIONS.VENTAS_APLICAR_DESCUENTO_ALTO)`. NO se toca `packages/pricing` ni `preview-service` (validan fuera del motor por diseño). `liquidarApartado` no necesita re-chequeo: solo materializa precios ya validados.
- **Fix aplicado**: `apartados/service.ts`: `ZERO = new Decimal(0)`; helper local privado `validarTopeDescuento(client, descuentoGlobalPct, permiteDescuentoAlto)` (línea 37) que retorna temprano si `permiteDescuentoAlto`/pct null-undefined/pct<=0, lee `configVentas.findFirst()` (default 100 = sin tope) y lanza `ApartadoError(400, ...)` si `pct > max`; `interface CrearApartadoOpts { permiteDescuentoAlto?: boolean }` (línea 58); `crearApartado` recibe `opts: CrearApartadoOpts = {}` y, tras validar sucursal y ANTES de `calcularPreview`, llama `await validarTopeDescuento(...)` (línea 191). `apartados/routes.ts`: import `{ PERMISSIONS, hasPermission }`; en `POST "/"` resuelve `permiteDescuentoAlto` (líneas 100-102) y lo pasa como `{ permiteDescuentoAlto }` a `crearApartado` (línea 105). El dueño / usuario con `VENTAS_APLICAR_DESCUENTO_ALTO` conserva la capacidad de exceder el tope (paridad con `crearVenta`).

### R8-04 · HIGH · El flujo B2B pedido→venta evade el tope de descuento global manual (misma raíz que apartados) (business-logic-abuse)

- **Archivos**: `apps/api/src/modules/tenant/ventas/service.ts`, `apps/api/src/modules/tenant/pedidos/service.ts`, `apps/api/src/modules/tenant/pedidos/routes.ts`, `apps/api/src/modules/tenant/cotizaciones/service.ts`, `apps/api/src/modules/tenant/cotizaciones/routes.ts`, `apps/api/src/modules/tenant/listas-precios/preview-service.ts`
- **Explotación**: `crearPedido()` (y `convertirCotizacionAPedido`, alimentado por `cotizaciones/service.ts`) pasaba el `descuentoGlobalPct` del cliente a `calcularPreview()` sin llamar a `validarTopeDescuento()` — el tope de `crearVenta` faltaba aquí también. `convertirAVenta()` → `persistirVentaDesdePedido()` escribe una venta `cobrada` `canal:'mayoreo'` directo desde los precios almacenados del pedido (NO re-enruta por `crearVenta`, el tope nunca se re-evalúa) y descuenta stock. Como el total del pedido ya viene reducido por el descuento sin cap, la validación de pago se satisface con el total deflactado (o cero). Un vendedor con `PEDIDOS_CREAR`/`PEDIDOS_CONVERTIR_VENTA` pero sin `VENTAS_APLICAR_DESCUENTO_ALTO` podía registrar ventas B2B a descuento arbitrario y, con `descuentoGlobalPct=100`, enviar mercancía gratis.
- **Decisión del panel**: Ganadora propuesta 3 (mayoría #2/#3). Exportar el `validarTopeDescuento` ya existente en `ventas/service.ts` (fuente única de verdad del tope, alimentada por `VENTAS_APLICAR_DESCUENTO_ALTO`) y aplicarlo en los DOS puntos donde el cliente inyecta `descuentoGlobalPct` (creación de pedido y de cotización). Capar en el origen cierra toda la cadena: `convertirCotizacionAPedido` y `convertirAVenta`/`persistirVentaDesdePedido` solo releen totales ya capados. El flag de permiso viaja por el INPUT (no por Zod → no spoofeable). Se revierte el helper redundante `evaluarTopeDescuentoGlobal` y el import de `decimal.js` en `preview-service.ts` (un solo mecanismo). NO se toca la conversión ni el motor de pricing.
- **Fix aplicado**:
  - `ventas/service.ts:59`: `async function validarTopeDescuento(` → `export async function validarTopeDescuento(` (cuerpo inline sin cambios; lee `config_ventas.descuentoMaximoPct`, default 100, retorna temprano si `permiteDescuentoAlto`/pct null-undefined/pct<=0, lanza `VentaError(400, ..., {descuentoMaximoPct, solicitado})`).
  - `pedidos/service.ts`: `import { VentaError, validarTopeDescuento } from "../ventas/service.js"`; campo `permiteDescuentoAlto?: boolean` en `CrearPedidoInput`; en `crearPedido`, ANTES de `calcularPreview` (línea 282), gate `try/catch` que traduce `VentaError → PedidoError(statusCode, message, extra)`.
  - `pedidos/routes.ts`: import `{ PERMISSIONS, hasPermission }`; en `POST "/"` resuelve `permiteDescuentoAlto` (línea 110) y lo pasa por el input a `crearPedido` (NO en el `pedidoCreateSchema` Zod).
  - `cotizaciones/service.ts`: mismo import; `permiteDescuentoAlto?: boolean` en `CrearCotizacionInput`; gate `try/catch` en `crearCotizacion` antes de `calcularPreview` (línea 182) traduciendo `VentaError → CotizacionError`.
  - `cotizaciones/routes.ts`: resuelve `permiteDescuentoAlto` (línea 98) y lo pasa por el input (no por Zod).
  - `preview-service.ts`: revertido a su estado original (removido `evaluarTopeDescuentoGlobal` y el import de `decimal.js`); el preview solo forwardea, el tope se aplica antes en el servicio de alta.
  - NO se tocan `convertirAVenta`/`persistirVentaDesdePedido`/`convertirCotizacionAPedido` (releen líneas ya capadas). Cumple TS strict/`exactOptionalPropertyTypes` (`permiteDescuentoAlto` siempre boolean definido; `descuentoGlobalPct` normalizado con `?? null`).

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores (incluye el fix de `test/helpers.ts` por el nuevo campo requerido `TRUST_PROXY_HOPS`).

## Ronda 9 — 2026-07-28

Rama: `seguridad/auditoria`. 1 vulnerabilidad aprobada por el panel y aplicada.

### R9-01 · MEDIUM · El tope global de uso de cupones de ecommerce (`cuponTenant`) es evadible vía race check-then-increment en el checkout (business-logic-abuse)

- **Archivos**: `apps/api/src/modules/tenant/checkout/cupon-service.ts`, `apps/api/src/modules/tenant/checkout/service.ts`
- **Explotación**: El flujo de cupones de la tienda validaba el tope total de uso de forma NO atómica. `evaluarCupon` leía la fila y rechazaba solo cuando `c.usosActuales >= c.usosTotal` (`cupon-service.ts:39-41`); el contador se incrementaba por separado y mucho después con `registrarUsoCupon`, un `updateMany({ data: { usosActuales: { increment: 1 } } })` incondicional (sin lock, sin `WHERE usosActuales < usosTotal`, sin transacción abarcando read→increment, y que además tragaba errores con `.catch`), invocado desde `iniciarCheckout` SOLO después de crear el pedido y el payment intent. Entre el read (línea 121) y el increment (líneas ~205-207) había un hueco async amplio (folio counter, pedido create, llamada externa `provider.crearIntent`, pedido update). Dos o más `iniciarCheckout` concurrentes leían el mismo `usosActuales`, todos pasaban el gate `>= usosTotal`, todos aplicaban el descuento (monto_fijo / porcentaje / envio_gratis) y todos incrementaban después, así que un cupón limitado a N usos se redimía en más de N pedidos. Se agravaba porque el carrito solo se marca `convertido` en la ruta de confirmación de pago (webhook), nunca en `iniciarCheckout`, permitiendo re-disparar el mismo carrito+cupón. Es la ruta de `cuponTenant` de ecommerce, distinta de la ya reportada en `promociones/service.ts`.
- **Decisión del panel**: Ganadora propuesta 1 (consenso unánime 3/3). Cerrar el TOCTOU del tope global con un compare-and-swap atómico a nivel de fila: reservar el uso ANTES de aplicar el descuento con un `updateMany` guardado por `usosActuales < usosTotal` (field reference de Prisma 6.6, columna vs columna, sin raw SQL) y liberarlo si la creación de pedido/intent falla, espejando el patrón CAS que el propio archivo ya usa en la confirmación de pago (`service.ts:331-334`). Postgres serializa el UPDATE guardado por fila: en N checkouts concurrentes solo los primeros `usosTotal` ganan (count===1) y aplican el descuento; el resto ve el cupón agotado y paga completo. La rama `usosTotal: null` cubre cupones ilimitados. No toca la ruta de webhook ni SQL raw; respeta TS strict y los conditional spreads por `exactOptionalPropertyTypes`.
- **Fix aplicado**:
  - `cupon-service.ts`: eliminado `registrarUsoCupon` (updateMany incondicional con `.catch` que tragaba errores). `evaluarCupon` conserva el read `usosActuales >= usosTotal` SOLO como rechazo rápido de UX; ya no es la autoridad del tope. Nuevo `reservarUsoCupon(client, codigo): Promise<boolean>` (línea 75): `updateMany({ where: { codigo, OR: [{ usosTotal: null }, { usosActuales: { lt: client.cuponTenant.fields.usosTotal } }] }, data: { usosActuales: { increment: 1 } } })`, retorna `res.count === 1`. Nuevo `liberarUsoCupon(client, codigo): Promise<void>` (línea 87): rollback best-effort `updateMany({ where: { codigo, usosActuales: { gt: 0 } }, data: { usosActuales: { decrement: 1 } } }).catch(() => {})`.
  - `checkout/service.ts`: import `{ evaluarCupon, liberarUsoCupon, reservarUsoCupon }`. En `iniciarCheckout` el bloque de cupón (líneas 123-136) reserva ANTES de aplicar descuento y condiciona toda la aplicación a ganar el CAS (`if (ev.valido && (await reservarUsoCupon(...)))`); el perdedor de la carrera ve el cupón agotado y paga completo. La creación de pedido + `provider.crearIntent` + update se extrajo a `crearPedidoConIntent(...)` (línea 163) y la llamada se envuelve en try/catch (líneas 139-151) que compensa la reserva con `liberarUsoCupon` ante fallo, preservando el semántico previo (checkout fallido NO consume uso). Eliminado el bloque final no-atómico `registrarUsoCupon(...)`.
  - Sin `any`; conditional spreads intactos por `exactOptionalPropertyTypes`. No se toca el path de webhook ni la marca `convertido` del carrito.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores. Las únicas referencias residuales a `registrarUsoCupon` viven en `apps/api/dist` (artefactos compilados, se regeneran en el build); la fuente está limpia.

---

## Ronda 10 — 2026-07-28

Rama: `seguridad/auditoria`. 1 vulnerabilidad aprobada por el panel y aplicada.

### R10-01 · MEDIUM · Las sesiones de los portales B2B/cliente/partner nunca se re-validan contra el estado activo de la cuenta o del tenant (sin ruta de revocación) (broken-access-control)

- **Archivos**: `apps/api/src/plugins/auth.ts`
- **Explotación**: A diferencia de la superficie POS/admin del tenant (`plugins/tenant-context.ts:48-67`), que recarga el usuario en cada request y rechaza usuarios inactivos y tenants cancelados, los portales de cliente/partner/B2B se apoyaban únicamente en los decoradores JWT `authenticateClienteB2b` / `authenticateCliente` / `authenticatePartner`. Esos decoradores solo ejecutaban `req.jwtVerify()` y comprobaban `req.user.kind`; nunca recargaban el principal para confirmar `clienteB2bUsuario.isActive` / `clienteB2b.isActive` / `partner.estado==='activo'` / `tenant.status!=='cancelled'`. `loginUsuarioB2b` aplicaba esos flags solo al momento del login, y no existía revocación server-side de tokens. Impacto concreto: una empresa mayorista da de baja a un comprador que dejó la compañía (`clienteB2bUsuario.isActive=false`), o el tenant se cancela por falta de pago, y cualquier JWT `cliente_b2b` ya emitido conservaba acceso total al portal — `POST /b2b-portal/pedidos` para colocar pedidos contra la línea de crédito de la empresa, leer estado-de-cuenta y precios personalizados — hasta que el token expirara naturalmente (`ACCESS_TOKEN_TTL_MIN`, default 15 min), sin forma de forzar el logout. El mismo patrón permitía a un partner suspendido seguir pegándole a `/partner/me`, `/commissions`, `/payouts`.
- **Decisión del panel**: Ganadora propuesta 1 (consenso unánime 3/3). Re-validar el principal en CADA request dentro de los decoradores de auth de portal, replicando el patrón ya probado en `plugins/tenant-context.ts`. Se corrige en el ÚNICO choke-point común a los tres portales, cubriendo todos los call sites (`b2b-portal/routes.ts:109`, cliente-portal, partner-portal) sin tocar handlers, rutas, servicios ni el shape de los tokens. Mínimo riesgo de regresión (un solo archivo, solo lecturas y condicionales, sin refresh/session store). Respeta la separación tenantPrisma/masterPrisma y usa campos verificados en schema (`Cliente.isActive`, `ClienteB2b.isActive`, `ClienteB2bUsuario.isActive`, `Partner.estado`, `Tenant.status`). Fuera de scope (no incluido): comparación `usuario.clienteB2bId` vs `req.user.clienteB2bId` para reasignación de empresa — no es la brecha reportada.
- **Fix aplicado**:
  - Import único `import { getTenantClient } from "@gaespos/db"` (`auth.ts:1`); `masterPrisma` vía `app.masterPrisma` (el plugin `auth` ya declara `dependencies: ["db"]`).
  - Helper local `tenantActivo(slug): Promise<boolean>` (línea 188) dentro de `authPlugin`, antes de los decoradores de portal: `app.masterPrisma.tenant.findUnique({ where: { slug } })` → `!!tenant && tenant.status !== "cancelled"` (mismo criterio que `tenant-context.ts:51`).
  - `authenticateCliente` (línea 193): tras `jwtVerify()` y check `kind === "cliente"`, rechaza si `!tenantActivo(tenantSlug)` ("Portal no disponible") y recarga `getTenantClient(tenantSlug).cliente.findUnique({ where: { id: sub }, select: { isActive: true } })`, rechazando si `!cliente || !cliente.isActive`.
  - `authenticatePartner` (línea 214): tras check `kind === "partner"` (partner es master-level, NO aplica tenant), recarga `app.masterPrisma.partner.findUnique({ where: { id: sub }, select: { estado: true } })`, rechazando si `!partner || partner.estado !== "activo"`.
  - `authenticateClienteB2b` (línea 232): tras check `kind === "cliente_b2b"`, rechaza si `!tenantActivo(tenantSlug)` y recarga `getTenantClient(tenantSlug).clienteB2bUsuario.findUnique({ where: { id: sub }, select: { isActive: true, clienteB2b: { select: { isActive: true } } } })`, rechazando si `!usuario || !usuario.isActive || !usuario.clienteB2b.isActive`. Cierra el impacto concreto: `POST /b2b-portal/pedidos`, `/estado-cuenta` y precios personalizados.
  - `getTenantClient(slug)` retorna el client de forma síncrona (sin `await`), igual que `tenant-context.ts:59`. `req.user` queda narrow por el check de `kind`, por lo que `tenantSlug`/`sub` están disponibles con TS strict sin `any`. No se toca Zod, rutas, servicios ni el shape de los tokens. Efecto: comprador dado de baja, empresa desactivada, cliente B2C inactivo, partner suspendido o tenant cancelado pierden acceso al portal en el próximo request, sin esperar al TTL del access token.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores.

---

## Ronda 11 — 2026-07-28

Rama: `seguridad/auditoria`. 4 vulnerabilidades aprobadas por el panel y aplicadas. (Otras vulnerabilidades en curso sobre la misma rama —doble timbrado CFDI, validación de payload offline en sync, revocación de refresh tokens de admin— pertenecen a rondas separadas y NO se incluyen en este commit.)

### R11-01 · HIGH · El descuento de stock de inventario es un read-compute-write no atómico: ventas concurrentes sobre-venden stock (race-condition)

- **Archivos**: `apps/api/src/modules/tenant/inventario/service.ts`
- **Explotación**: `aplicarAjuste` (única primitiva de mutación de stock usada por el path de venta POS) leía el stock actual con `findUnique`, calculaba el saldo nuevo en JS (`stockNuevo = stockActual + delta`), validaba `stockNuevo >= 0` contra ese snapshot y escribía el resultado como valor ABSOLUTO literal (`data: { stockActual: stockNuevo.toString() }`). Lost-update clásico. `crearVenta` lo invoca vía `persistirVenta → descontarStockLineas` dentro de `client.$transaction(...)` SIN `isolationLevel` (READ COMMITTED por defecto en Postgres) y SIN lock de fila/advisory sobre la variante (`lockVenta` está keyed por ventaId y solo lo usa `cobrarVenta`). Dos transacciones concurrentes validan cada una contra su snapshot stale y sobreescriben con un literal precomputado, derrotando el guard `InsufficientStockError`. Con stock=1, dos `POST /ventas` de 1 unidad cada una: ambas leen 1, ambas calculan 0 (pasa `>= 0`), ambas ejecutan `SET stockActual='0'`; ambas ventas commitean y se entregan 2 unidades existiendo 1. Escala con stock mayor (stock=10, dos ventas de 8 → 16 vendidas, stock queda en 2). Mismo patrón defectuoso afectaba a `aplicarTransferencia` (salida de origen) y `aplicarReservaApartado`/`liberarReservaApartado`. El resto del código (monedero `updateMany gte/decrement`, cupón, claim de pedido) ya usa compare-and-swap atómico; inventario era la excepción.
- **Decisión del panel**: Ganadora propuesta 2. Las tres coinciden en el CAS del path de venta (`updateMany gte/decrement` + `count===0 → InsufficientStockError`, espejando el patrón del monedero). La divergencia estaba solo en la reserva de apartado, cuya guarda compara DOS columnas (`stock_actual - stock_reservado >= cantidad`), inexpresable como `updateMany` tipado en Prisma. Las propuestas 1 y 3 usaban `pg_advisory_xact_lock` por variante+sucursal conservando el read-compute-write, pero ese lock SOLO lo toman las reservas (no `aplicarAjuste`, que decrementa `stock_actual` con CAS sin tomar el lock), dejando un hueco real: una venta concurrente que baja `stock_actual` entre el read y el write de la reserva permite sobre-reservar contra un stock stale. La propuesta 2 usa un CAS real vía `$executeRaw` sobre el invariante de dos columnas: bajo READ COMMITTED el `UPDATE` condicional re-evalúa el `WHERE` contra la fila commiteada tras liberarse el lock de fila (EvalPlanQual), cerrando también el cruce reserva-vs-venta. Lock-free, coherente con la filosofía CAS del resto del codebase, elimina el helper muerto `lockStock`/`RESERVA_STOCK_LOCK_NAMESPACE` (regla 7: no dead code), `$executeRaw` parametrizado (sin inyección) y `inventario_sucursal` resuelto por `search_path` del schema-per-tenant.
- **Fix aplicado**:
  - `aplicarAjuste`: tras `ensureInventario`, se ramifica por signo del delta. `delta < 0`: `updateMany({ where: { varianteId, sucursalId, stockActual: { gte: cantidadAbs } }, data: { stockActual: { decrement: cantidadAbs } } })`; si `count === 0`, refetch y `throw InsufficientStockError`. `delta > 0`: `update({ data: { stockActual: { increment: delta } } })`. `delta === 0`: no toca stock. El `inventarioMovimiento.create` conserva `cantidad: delta.toString()` firmado y los conditional spreads por `exactOptionalPropertyTypes`.
  - `aplicarTransferencia`: salida del origen con el mismo CAS `updateMany gte/decrement` + `count===0 → InsufficientStockError`; entrada al destino con `{ increment: cantidad }`. Movimientos salida/entrada y enlace recíproco `referenciaId` sin cambios.
  - `aplicarReservaApartado`: CAS atómico de dos columnas vía `$executeRaw`: `UPDATE inventario_sucursal SET stock_reservado = stock_reservado + $cantidad::numeric WHERE variante_id = $... AND sucursal_id = $... AND stock_actual - stock_reservado >= $cantidad::numeric`. Si `reservadas === 0`, `findUnique` solo para el mensaje, calcula `disponible = stockActual - stockReservado` y lanza `InsufficientStockError`. Conserva movimiento `apartado_reservado`.
  - `liberarReservaApartado`: decremento relativo atómico con clamp `GREATEST(stock_reservado - $cantidad::numeric, 0)` vía `$executeRaw`, sin lock. Conserva movimiento `apartado_liberado` con cantidad negada.
  - Eliminados por completo el helper `lockStock` y la constante `RESERVA_STOCK_LOCK_NAMESPACE` (dead code). TS strict sin `any`; firmas públicas y callers (`crearVenta → persistirVenta → descontarStockLineas`) sin cambios.

### R11-02 · MEDIUM · Autofactura pública: emisión de CFDI de cualquier venta a un RFC arbitrario conociendo solo el id (IDOR sin segundo factor) + oráculo GET sin auth ni rate-limit (idor)

- **Archivos**: `apps/api/src/modules/autofactura/routes.ts`, `apps/api/test/tenant-autofactura.test.ts`
- **Explotación**: El endpoint público `POST /autofactura/:tenantSlug/venta/:ventaId` resolvía la venta directamente por su id (`client.venta.findUnique({ where: { id: ventaId } })`) sin verificar que el solicitante fuera el comprador legítimo. La única "credencial" era el id de la venta (un CUID, no criptográficamente aleatorio, que por diseño se imprime/codifica en el QR del ticket). Quien obtuviera ese id (ticket fotografiado, tirado, mostrado en pantalla) podía: (1) emitir el CFDI de esa venta a un RFC de su elección —los datos fiscales del receptor vienen 100% del body— consumiendo un timbre del PAC/Facturama del negocio; y (2) una vez emitido, `evaluar()` marca `yaFacturada=true` y bloquea permanentemente que el comprador real facture su compra. Además el `GET` NO tenía rate-limit (el POST sí, 10/min) y devolvía sin auth el total, la fecha y el estado de facturabilidad de cualquier id — un oráculo de información fiscal/comercial. La ruta se registra sin `preHandler` de auth y sin `requirePerm`. Existía un mecanismo latente de segundo factor (`autofacturaToken` + `generateAutofacturaToken`) pero nunca se llamaba en `src/`.
- **Decisión del panel**: Ganadora propuesta 1 (consenso unánime 3/3). Segundo factor obligatorio (folio impreso + total del ticket) que NO viaja en el QR (el QR solo lleva el CUID), rate-limit al GET (10/min, igual que el POST) y cierre del oráculo con respuesta genérica que no distingue "no existe" de "folio/total incorrectos". Se corona la propuesta 1 por ser autoría primaria y limpia, entregar el par completo `routes.ts` + tests con cobertura de los casos clave, no tocar `cfdiEmitirSchema` (compartido con la ruta autenticada) ni `emitirCfdi`, usar `masterPrisma` para el tenant-by-slug y `getTenantClient` para la venta, y responder con `null` explícito por `exactOptionalPropertyTypes`. Sin tokens nuevos ni cambios de modelo de datos.
- **Fix aplicado**:
  - `routes.ts`: `segundoFactorSchema = z.object({ folio: z.string().trim().min(1).max(40), total: z.coerce.number().finite().nonnegative() })` (`coerce` porque en GET llega por query y en POST puede llegar como number/string). `interface VentaAutofactura` += `folio` y `select` de `cargarContexto` += `folio: true`. Helper `coincideSegundoFactor(venta, folio, total)`: `folioOk` case-insensitive + `totalOk` comparando `Number(venta.total).toFixed(2) === total.toFixed(2)` (evita falsos negativos Decimal vs number). GET: añade `config.rateLimit { max: 10, timeWindow: '1 minute' }`, hace `safeParse(req.query)` y, si `!success || !venta || !coincide`, responde payload genérico `{ negocio, ventaId, total: null, fecha: null, facturable: false, motivo: 'Verifica el folio y el total de tu ticket', yaFacturada: false, expiraAt: null }` idéntico exista o no la venta (oráculo cerrado); solo con coincidencia revela total/fecha/estado real vía `evaluar()`. POST: además de `cfdiEmitirSchema.parse(req.body)` hace `segundoFactorSchema.parse(req.body)`; tras `cargarContexto`, si `!venta || !cfg || !coincide` retorna `fail(reply, 404, ...)` ANTES de tocar el PAC. Se conserva `evaluar()` y el guard `yaFacturada` (409). `emitirCfdi` sigue recibiendo exactamente `{ ...body, ventaId, emitidoPorId, esAutofactura: true }`: folio/total se parsean por separado y NO se mergean, así no cambia la firma fiscal.
  - `test/tenant-autofactura.test.ts`: `cobrarVenta()` retorna `{ ventaId, folio }`. Casos: GET con folio+total correctos → facturable (total revelado); GET sin factor → `total:null`; GET total incorrecto → `total:null`; POST 201 con folio+total correctos y segundo POST → 409; POST total no coincide → 404; folio no coincide → 404; venta inexistente → 404.

### R11-03 · MEDIUM · Verificación HMAC de webhooks Skydropx/Envía falla-abierto cuando el secreto de la paquetería está vacío (default inseguro `?? ""`) (crypto-fail-open)

- **Archivos**: `packages/paqueterias/src/skydropx.ts`, `packages/paqueterias/src/envia.ts`
- **Explotación**: `SkydropxClient.parseWebhook` y `EnviaClient.parseWebhook` computaban `createHmac("sha256", this.webhookSecret)` y lo comparaban con `timingSafeEqual`, pero ninguno guardaba contra un secreto VACÍO. La factory (`apps/api/src/plugins/paqueterias.ts`) defaultea el secreto a `""` (`process.env.SKYDROPX_WEBHOOK_SECRET ?? ""`, `ENVIA_WEBHOOK_SECRET ?? ""`), estado de producción muy plausible (la paquetería es opcional y un tenant suele usar una sola). Con secreto vacío el HMAC se computa con clave `""` — valor que cualquier atacante puede computar deterministamente (`sha256=HMAC-SHA256("", payload)`) — así que `timingSafeEqual` pasa. Los verificadores hermanos fallan-cerrado explícitamente (`stripe-sig.ts` y `conekta.ts` lanzan `if (!secret)`); Skydropx/Envía omitían ese guard. La ruta pública `POST /envios/webhook` toma `paqueteria`/`payload`/`signature` del body y llama `provider.parseWebhook(...)` sin `requirePerm`, así que un usuario autenticado del tenant (el `tenant-context` preHandler exige JWT `kind==="tenant"`) que elija la paquetería no configurada podía inyectar eventos de tracking forjados en `procesarWebhookEnvio`, mutando el ciclo de vida del pedido (marcar `entregado`, `en_camino`, `envio_excepcion`).
- **Decisión del panel**: Ganadora propuesta 1. Espejar el patrón fail-closed de `stripe-sig.ts`/`conekta.ts`: lanzar ante secreto vacío en ambos `parseWebhook`, cortando el anti-patrón cripto raíz (un HMAC que pasa con una clave vacía conocida por el atacante en lugar de negarse a verificar).
- **Fix aplicado**:
  - `skydropx.ts` y `envia.ts`: al inicio de `parseWebhook`, guard fail-closed `if (!this.webhookSecret) throw new PaqueteriaError("Webhook <carrier> sin secreto configurado", "INVALID_WEBHOOK")` antes de computar/comparar el HMAC. El resto de la verificación (`timingSafeEqual` de la firma esperada) sin cambios. Un secreto vacío o `undefined` ahora hace fallar-cerrado en lugar de autenticar con clave vacía; la ruta `POST /envios/webhook` traduce el `PaqueteriaError` a un 400 genérico. Sin `any`.

### R11-04 · LOW · Endpoints de lectura de plataforma gateados solo por `authenticateAdmin` (sin check de rol): un admin support/billing de bajo privilegio lee metadata de tarjetas cross-tenant y el log completo de auditoría (broken-function-level-authorization)

- **Archivos**: `apps/api/src/modules/admin/tenants-routes.ts`, `apps/api/src/modules/admin/audit-routes.ts`
- **Explotación**: `authenticateAdmin` solo asegura `req.user.kind==='admin'` — NO chequea `req.user.role`. `adminTenantsRoutes` gatea toda MUTACIÓN tras `requireSuperadmin` (POST /, PATCH estado/plan/dunning), probando que support/billing son estrictamente menor privilegio, pero los endpoints de lectura del mismo archivo lo omitían: `GET /:slug/detalle` devolvía la metadata del método de pago de cada tenant (last4, brand, expMonth, expYear) y la config de dunning/auto-charge a CUALQUIER rol admin. La misma brecha existía en `adminAuditRoutes` (preHandler bare `authenticateAdmin`), exponiendo el audit trail de seguridad cross-tenant (`admin.login`, `admin.mfa_backup_code_used`, `tenant.onboarded` con correos de dueños, IPs de actores) a roles support/billing. Un usuario `support` logueado obtenía un token `kind:admin role:'support'` y podía enumerar todos los tenants, cosechar last4/brand/expiry de cada uno y leer el log de eventos de seguridad de toda la plataforma. Data expuesta = metadata enmascarada (sin PAN/CVV) y el actor ya es un admin interno de confianza → severidad LOW (fuga vertical de privilegio insider).
- **Decisión del panel**: Ganadora propuesta 1 (consenso 3/3). Autorización por rol (no solo `kind`) en los dos endpoints de LECTURA sensibles, reusando el patrón in-repo (`billing-ops-routes.ts` `ROLES_COBRANZA`, `requireSuperadmin`): (a) cerrar `/:slug/detalle` con `requireCobranza` (roles superadmin+billing); (b) restringir todo el audit log a superadmin vía un segundo preHandler; (c) dejar a propósito `GET /` (listado de tenants) accesible a support porque no expone tarjeta ni PII (solo slug/name/status/plan/vertical/fechas) y es su directorio legítimo de soporte. No toca `authenticateAdmin` (evita regresión en todos los módulos admin), narrowing por `req.user.kind==='admin'` antes de leer `req.user.role` (TS strict, discriminated union, sin `any`), `masterPrisma`, mutaciones intactas con `requireSuperadmin`.
- **Fix aplicado**:
  - `tenants-routes.ts`: a nivel de módulo `const ROLES_COBRANZA = new Set(["superadmin", "billing"])`; helper `requireCobranza(req, reply)` (`req.user.kind === "admin" && ROLES_COBRANZA.has(req.user.role)` → true, si no 403). Primera línea del handler `GET /:slug/detalle`: `if (!requireCobranza(req, reply)) return;`, cerrando la fuga de `paymentMethod` (last4/brand/expMonth/expYear) + `dunningPolicy`/`autoChargeEnabled` hacia support. Mutaciones y `GET /` sin cambios.
  - `audit-routes.ts`: segundo preHandler tras `app.authenticateAdmin` que retorna si `req.user.kind === "admin" && req.user.role === "superadmin"`, si no 403 ("Solo un superadmin puede consultar el log de auditoría"). Comentario doc ajustado a "Solo lectura, solo superadmin".
  - Una sola declaración de cada símbolo (sin duplicados por edición concurrente); TS strict con narrowing de discriminated union sin `any`; `masterPrisma` para dato de plataforma.

**Verificación**: `pnpm --filter @gaespos/api exec tsc --noEmit` → 0 errores. `pnpm --filter @gaespos/paqueterias exec tsc --noEmit` → 0 errores.

---
